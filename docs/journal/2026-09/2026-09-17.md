
## Revisión del inventario de blobs (6b) — Miguel
- Quién: Miguel
- Dónde: storage account `containerproduction2` (RG prod-reachu), container `inventory-reports`
- Cuándo: 2026-09-17 10:00 (Oslo)
- Contexto: punto 6b de `infrastructure/cost-audit-2026-09-16.md`: hace falta el primer reporte del inventario para medir qué se puede borrar.
- Hecho: el container sigue vacío y todavía no se generó ningún reporte. La regla `inventario-uploads-2026-09` está activa (Weekly, CSV). Queda una nueva revisión a las 15:00.
- Pendiente: calcular los números cuando llegue el reporte. Para el conjunto a conservar se necesita la MySQL de prod, que está apagada desde el 16/09: hay que encenderla temporalmente o usar un backup, y decidirlo con Angelo.

## Export diario de costos Azure + cron de reporte — miguel
- Quién: miguel (lo pidió Angelo)
- Dónde: sub `3d276f7e…` (Azure Sponsorship, ahora con quotaId PayAsYouGo / agreement WebDirect); storage `containerproduction2` (RG prod-reachu), container `cost-exports`
- Cuándo: 2026-09-17 ~12:20
- Contexto: Angelo quiere ver el gasto diario exacto. Cost Management Query y Consumption usageDetails todavía no devuelven filas.
- Hecho:
  - Se registró el provider `Microsoft.CostManagementExports`.
  - Se creó el export `costo-diario`: diario, `MonthToDate`, CSV particionado, `OverwritePreviousReport`, destino `cost-exports/azure/`. En WebDirect, `ActualCost` no se admite: hay que usar `type=Usage` con `dataVersion=2019-11-01` (las demás versiones se rechazan).
  - Se asignó `Storage Blob Data Contributor` a la identidad del export (principal `a8ed0102…`) sobre el storage.
  - Se lanzó un run on-demand. Quedó `Queued`.
  - Script `workspace-miguel/scripts/azure-cost-daily.sh`: lee el último CSV y reporta el gasto de ayer, desglosado por RG.
  - Cron OpenClaw `azure-cost-daily` (09:30 Oslo, payload command): entrega el reporte por Discord DM a Angelo.
- Pendiente: confirmar que el export genera archivos cuando Azure publique los datos. Si dentro de unos días sigue vacío, revisar el `runHistory` del export.

## Qué sigue facturando con prod apagado y QA apagado de noche — miguel
- Quién: miguel (lo preguntó Angelo)
- Dónde: sub `3d276f7e…`, revisado en vivo (sin cambios)
- Cuándo: 2026-09-17 ~13:10
- Contexto: prod está apagado bajo demanda desde el 16/09 18:58 y el cluster QA se apaga de noche.
- Hecho: revisión read-only con precios de la Retail Prices API (norwayeast). Mientras prod está apagado sigue cobrando:
  - Redis `redus-vio-prod`: Azure Managed Redis Balanced B1, no se puede pausar. $0,197/h, ~$144/mes. Es la mayor parte del gasto.
  - Load balancer del AKS prod (3 reglas): ~$18/mes.
  - 2 IPs estáticas (`aks-outbound-vio-prod`, `nginx-ingress-vio-prod`): ~$7/mes.
  - Storage de las DBs: PG prod de 64 GB, ~$10/mes; MySQL prod de 20 GB, ~$3/mes.
  - ACR `reachuprod2` (Standard): ~$20/mes.
  - No cobran: la compute del AKS (VMSS en 0, control plane en tier Free) ni las Container Apps de prod (Stopped).
  - Staging: `vio-ecom-db-staging` (D2ds_v4, ~$167/mes) y `redus-vio-staging` (~$144/mes) quedan prendidos 24/7, aunque `kubernetesqa` se apaga de noche y el fin de semana.
- Pendiente: decide Angelo. Opciones: borrar o recrear el Redis de prod bajo demanda; apagar la MySQL de staging junto con el cluster QA (~$80/mes).
