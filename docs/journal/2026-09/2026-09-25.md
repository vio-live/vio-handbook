# 2026-09-25

## Fix cart indicator itemCount for multi-sponsor carts + cleanup-01 (debug prints) — Maxi

- Contexto: briefing del día pedía (1) corregir el indicador de itemCount en el cart overlay multi-sponsor, y (2) cleanup-01: limpiar debug prints en el SDK. Investigando el repo VioSwiftSDK encontré que el fix para (1) ya existía en la branch estancada `feat/multi-sponsor-checkout-flow` (commits `172ae52` y `efc2e97`), pero esa branch completa incluye trabajo grande y no probado de Apple Pay/Klarna/Vipps scoping (ver `Q4-L4-HANDOFF.md` en esa branch) que no es seguro mergear sin testing en dispositivo.
- Hecho:
  - Cherry-pick manual y aislado del patrón de fix ya validado, aplicado directo sobre `develop` en una branch nueva `fix/cart-indicator-itemcount-and-debug-prints`:
    - `CartModule.itemCount` ahora suma legacy `items` + `itemCountAcrossSponsors` (antes solo contaba legacy, así que el indicador flotante mostraba 0 en multi-sponsor).
    - Nuevos accessors `totalAcrossAllCarts` / `currencyAcrossAllCarts` en `CartModule+SponsorCart.swift`, usados por `VFloatingCartIndicator` en vez de `cartTotal`/`currency` (legacy-only).
  - Cleanup-01: reemplazados ~90 `print()` sueltos por `VioLogger.debug/warning/error` en `PaymentModule`, `GraphQLHTTPClient`, `ConfigAPIClient`, `CampaignManager`, `CampaignWebSocketManager`, `VioCampaignPartnerAPI`, `VioPlacementManifestUploader`, `ProductService`, `VApplePayButton`, `VioColors`, `VioDesignSystem`. De paso se eliminó el dump completo del payload de backend + el email del cliente que se logueaban sin gate en `klarnaNativeInit` (ahora solo booleano `emailPresent`, igual que en `applePayConfirm`). Quedaron fuera de scope: prints dentro de `#Preview` (son stubs normales de SwiftUI), `VioNetwork`/`VioTesting` (targets sin dependencia a VioCore, no pueden usar VioLogger sin tocar el grafo de módulos), y `VioCastingUI` (área de casting en vivo, no relacionada al checkout).
  - Rebase sobre `origin/develop` (había avanzado con `feat(analytics): VioAnalyticsClient` #15 mientras trabajaba), push de la branch, y PR abierto contra `develop`: https://github.com/vio-live/VioSwiftSDK/pull/17
- Pendiente:
  - `swift build` local se colgó sin red aparente para resolver dependencias (Apollo/Mixpanel/Nuke/Stripe-iOS) — lo maté tras ~13min. `git push`/`gh pr create` sí funcionaron bien, así que no fue falta de red real; no identifiqué la causa del cuelgue. Solo alcancé a validar sintaxis con `swiftc -parse` archivo por archivo (limpio salvo 5 errores de coma final pre-existentes en `CampaignManager.swift`, no relacionados a mis cambios — toolchain viejo del CommandLineTools vs. trailing commas, no algo que rompa el build real en Xcode).
  - El PR #17 queda marcado explícitamente como "no verificado con build real" — falta build en Xcode/simulador y smoke test del indicador con carritos multi-sponsor antes de mergear.
  - La branch `feat/multi-sponsor-checkout-flow` (Q4-L4, Apple Pay/Klarna/Vipps por sponsor) sigue pausada y sin mergear — no tocada en este pase.

## Revisión de gastos Azure con datos reales de facturación — Miguel
- Quién: Miguel (a pedido de Angelo)
- Dónde: subscription `3d276f7e-0783-4581-8a49-ad0a2c432c63` (Azure Sponsorship), Cost Management API
- Cuándo: 2026-09-25 ~09:30
- Contexto: Angelo preguntó cómo vienen los gastos. Es la primera vez que Cost Management devuelve datos para esta suscripción (antes daba vacío) — hay facturación real desde el 17/09, cuando se agotaron los créditos.

### Datos
Moneda de facturación: NOK. Tipo usado: 9,492 NOK/USD (2026-09-24).

Costo diario de septiembre (NOK):
| Día | 17 | 18 | 19 | 20 | 21 | 22 | 23 | 24 |
|---|---|---|---|---|---|---|---|---|
| NOK | 342 | 465 | 521 | 520 | 639 | 742 | 673 | 482* |

\* el 24/09 está incompleto (todos los recursos de costo fijo bajan ~30 % uniforme). **El último día completo y fiable es el 23/09: 673 NOK/día ≈ $2.155/mes de run-rate.**

El salto del 20→22 es el encendido de commerce prod (node pool pasa de 6 a 216 NOK/día): +~$680/mes. Fue decisión de Angelo (3 clientes sincronizando), no es una anomalía.

Desglose por recurso, día completo 23/09:
| Recurso | NOK/día | USD/mes |
|---|---|---|
| AKS prod (3×D4as_v5 + discos) | 213 | 683 |
| AKS QA (3×E2as_v4 + discos) | 112 | 359 |
| redus-vio-prod | 96 | 308 |
| vio-ecom-db-prod (MySQL D2ds_v4) | 74 | 237 |
| vio-ecom-db-staging (MySQL B2s) | 43 | 137 |
| LoadBalancers k8s | 16 | 51 |
| redus-vio-staging | 15 | 47 |
| vm-clickhouse-vio | 13 | 41 |
| capp-svc-lb | 12 | 39 |
| prod-cdn | 12 | 37 |
| containerproduction2 (blobs) | 11 | 34 |
| reachuqa2 + reachuprod2 (ACR) | 14 | 45 |
| pg-api-vio-staging | 7 | 22 |
| resto (<$15 c/u) | ~35 | ~115 |
| **TOTAL** | **673** | **2.155** |

### Hallazgos nuevos
1. **`pg-api-vio-production` se despertó sola el 23/09, no el 16/10.** Estaba Stopped desde el 16/09. Azure PostgreSQL Flexible Server sólo admite 7 días de stop y después arranca solo — no 30 (eso es MySQL). El costo pasó de 3 a 37 NOK/día el 24/09 (día parcial; a día completo ~53 NOK ≈ **$170/mes**). `ca-api-vio-production` y `ca-analytics-vio-production` siguen **Stopped**: la base está facturando sin que nadie la use. No se toca sin OK de Angelo (prod es su decisión).
2. **`redus-vio-prod` es `Microsoft.Cache/redisEnterprise`, SKU `Balanced_B1`, no Azure Cache for Redis básico.** Cuesta **$308/mes**, el recurso individual más caro después de los node pools. El cost-audit del 16/09 lo trataba como "Redis B1 → B0, ~$87/mes": ese número está mal, el ahorro potencial es mucho mayor. Requiere revisar de nuevo el punto correspondiente del audit.
3. La MySQL de staging en Burstable B2s cuesta $137/mes, no los ~$96 estimados (la diferencia es storage/backup/IOPS, no compute).
4. El scheduler de QA funciona: el node pool bajó de 169 NOK/día (19–21/09) a 112 NOK/día el 23/09, ~-35 %.
5. `claude-trader-rg` + `claude-trader-plan` ≈ $27/mes — es de Angelo, no de Vio.

### Pendiente
- Avisado a Angelo de lo de `pg-api-vio-production` (decide él).
- Corregir el punto de Redis en `docs/infrastructure/cost-audit-2026-09-16.md` con el costo real de $308/mes.
- Los puntos 1/2/3 de staging/QA siguen esperando OK y no se ejecutó nada.
