# 2026-09-25

## Fix cart indicator itemCount for multi-sponsor carts + cleanup-01 (debug prints) — Maxi

- Contexto: briefing del día pedía (1) corregir el indicador de itemCount en el cart overlay multi-sponsor, y (2) cleanup-01: limpiar debug prints en el SDK. Investigando el repo VioSwiftSDK encontré que el fix para (1) ya existía en la branch estancada `feat/multi-sponsor-checkout-flow` (commits `172ae52` y `efc2e97`), pero esa branch completa incluye trabajo grande y no probado de Apple Pay/Klarna/Vipps scoping (ver `Q4-L4-HANDOFF.md` en esa branch) que no es seguro mergear sin testing en dispositivo.
- Hecho:
  - Cherry-pick manual y aislado del patrón de fix ya validado, aplicado directo sobre `develop` en una branch nueva `fix/cart-indicator-itemcount-and-debug-prints`:
    - `CartModule.itemCount` ahora suma legacy `items` + `itemCountAcrossSponsors` (antes solo contaba legacy, así que el indicador flotante mostraba 0 en multi-sponsor).
    - Nuevos accessors `totalAcrossAllCarts` / `currencyAcrossAllCarts` en `CartModule+SponsorCart.swift`, usados por `VFloatingCartIndicator` en vez de `cartTotal`/`currency` (legacy-only).
  - Cleanup-01: reemplazados ~90 `print()` sueltos por `VioLogger.debug/warning/error` en `PaymentModule`, `GraphQLHTTPClient`, `ConfigAPIClient`, `CampaignManager`, `CampaignWebSocketManager`, `VioCampaignPartnerAPI`, `VioPlacementManifestUploader`, `ProductService`, `VApplePayButton`, `VioColors`, `VioDesignSystem`. De paso se eliminó el dump completo del payload de backend + el email del cliente que se logueaban sin gate en `klarnaNativeInit` (ahora solo booleano `emailPresent`, igual que en `applePayConfirm`). Quedaron fuera de scope: prints dentro de `#Preview` (son stubs normales de SwiftUI), `VioNetwork`/`VioTesting` (targets sin dependencia a VioCore, no pueden usar VioLogger sin tocar el grafo de módulos), y `VioCastingUI` (área de casting en vivo, no relacionada al checkout).
  - Rebase sobre `origin/develop` (había avanzado con `feat(analytics): VioAnalyticsClient` #15 mientras trabajaba), push de la branch, y PR abierto contra `develop`: https://github.com/vio-live/VioSwiftSDK/pull/17
- Pendiente:
  - `swift build` local se colgó sin red aparente para resolver dependencias (Apollo/Mixpanel/Nuke/Stripe-iOS) — lo maté tras ~13min. `git push`/`gh pr create` sí funcionaron bien, así que no fue falta de red real; no identifiqué la causa del cuelgue. Solo alcancé a validar sintaxis con `swiftc -parse` archivo por archivo (limpio salvo 5 errores de coma final pre-existentes en `CampaignManager.swift`, no relacionados a mis cambios — toolchain viejo del CommandLineTools vs. trailing commas, no algo que rompa el build real en Xcode).
  - El PR #17 queda marcado explícitamente como "no verificado con build real" — falta build en Xcode/simulador y smoke test del indicador con carritos multi-sponsor antes de mergear.
  - La branch `feat/multi-sponsor-checkout-flow` (Q4-L4, Apple Pay/Klarna/Vipps por sponsor) sigue pausada y sin mergear — no tocada en este pase.

## Revisión de gastos Azure con datos reales de facturación — Miguel
- Quién: Miguel (a pedido de Angelo)
- Dónde: subscription `3d276f7e-0783-4581-8a49-ad0a2c432c63` (Azure Sponsorship), Cost Management API
- Cuándo: 2026-09-25 ~09:30
- Contexto: Angelo preguntó cómo vienen los gastos. Es la primera vez que Cost Management devuelve datos para esta suscripción (antes daba vacío) — hay facturación real desde el 17/09, cuando se agotaron los créditos.

### Datos
Moneda de facturación: NOK. Tipo usado: 9,492 NOK/USD (2026-09-24).

Costo diario de septiembre (NOK):
| Día | 17 | 18 | 19 | 20 | 21 | 22 | 23 | 24 |
|---|---|---|---|---|---|---|---|---|
| NOK | 342 | 465 | 521 | 520 | 639 | 742 | 673 | 482* |

\* el 24/09 está incompleto (todos los recursos de costo fijo bajan ~30 % uniforme). **El último día completo y fiable es el 23/09: 673 NOK/día ≈ $2.155/mes de run-rate.**

El salto del 20→22 es el encendido de commerce prod (node pool pasa de 6 a 216 NOK/día): +~$680/mes. Fue decisión de Angelo (3 clientes sincronizando), no es una anomalía.

Desglose por recurso, día completo 23/09:
| Recurso | NOK/día | USD/mes |
|---|---|---|
| AKS prod (3×D4as_v5 + discos) | 213 | 683 |
| AKS QA (3×E2as_v4 + discos) | 112 | 359 |
| redus-vio-prod | 96 | 308 |
| vio-ecom-db-prod (MySQL D2ds_v4) | 74 | 237 |
| vio-ecom-db-staging (MySQL B2s) | 43 | 137 |
| LoadBalancers k8s | 16 | 51 |
| redus-vio-staging | 15 | 47 |
| vm-clickhouse-vio | 13 | 41 |
| capp-svc-lb | 12 | 39 |
| prod-cdn | 12 | 37 |
| containerproduction2 (blobs) | 11 | 34 |
| reachuqa2 + reachuprod2 (ACR) | 14 | 45 |
| pg-api-vio-staging | 7 | 22 |
| resto (<$15 c/u) | ~35 | ~115 |
| **TOTAL** | **673** | **2.155** |

### Hallazgos nuevos
1. **`pg-api-vio-production` se despertó sola el 23/09, no el 16/10.** Estaba Stopped desde el 16/09. Azure PostgreSQL Flexible Server sólo admite 7 días de stop y después arranca solo — no 30 (eso es MySQL). El costo pasó de 3 a 37 NOK/día el 24/09 (día parcial; a día completo ~53 NOK ≈ **$170/mes**). `ca-api-vio-production` y `ca-analytics-vio-production` siguen **Stopped**: la base está facturando sin que nadie la use. No se toca sin OK de Angelo (prod es su decisión).
2. **`redus-vio-prod` es `Microsoft.Cache/redisEnterprise`, SKU `Balanced_B1`, no Azure Cache for Redis básico.** Cuesta **$308/mes**, el recurso individual más caro después de los node pools. El cost-audit del 16/09 lo trataba como "Redis B1 → B0, ~$87/mes": ese número está mal, el ahorro potencial es mucho mayor. Requiere revisar de nuevo el punto correspondiente del audit.
3. La MySQL de staging en Burstable B2s cuesta $137/mes, no los ~$96 estimados (la diferencia es storage/backup/IOPS, no compute).
4. El scheduler de QA funciona: el node pool bajó de 169 NOK/día (19–21/09) a 112 NOK/día el 23/09, ~-35 %.
5. `claude-trader-rg` + `claude-trader-plan` ≈ $27/mes — es de Angelo, no de Vio.

### Pendiente
- Avisado a Angelo de lo de `pg-api-vio-production` (decide él).
- Corregir el punto de Redis en `docs/infrastructure/cost-audit-2026-09-16.md` con el costo real de $308/mes.
- Los puntos 1/2/3 de staging/QA siguen esperando OK y no se ejecutó nada.

## Ejecución de medidas de ahorro (staging/QA + desperdicio) — Miguel
- Quién: Miguel. Angelo dijo "tenemos que tomar medidas" tras la revisión de gastos de esta mañana.
- Dónde: sub `3d276f7e…`. RGs `qa`, `rg-vio-databases`, `rg-api-vio-production`.
- Cuándo: 2026-09-25 09:45–10:35
- Contexto: run-rate real medido en $2.155/mes. **Prod de commerce NO se tocó** (AKS, MySQL prod, Redis prod): hay 3 clientes sincronizando y Angelo no dio OK explícito para prod. Sólo se ejecutó desperdicio puro + staging/QA.

### 1. `pg-api-vio-production` apagada + guardián diario
Estaba `Ready` facturando ~$170/mes con `ca-api-vio-production` y `ca-analytics-vio-production` en `Stopped`: nadie la usaba.
- `az postgres flexible-server stop` → `Stopped`. Azure confirmó en el mismo output el límite de **7 días**, no 30.
- Como el auto-start volvería el ~02/10, se creó el Container Apps Job **`job-pg-prod-stop-guard`** (`30 4 * * *`, env `cae-qa-ops`): si la encuentra `Ready` la apaga, si no no hace nada. Probado con `job start`: `inicial=Stopped / accion=ninguna / final=Stopped`, Succeeded.
- Permiso mínimo: rol custom **`PostgreSQL Stop Guard (pg-api-vio-production)`** (sólo `read` + `stop/action`), asignado a `id-qa-aks-scheduler` con scope al servidor.

### 2. Node pool de QA: 3×E2as_v4 → 2×B2ms
Evidencia: requests 2,67 cores y uso real 0,5; RAM de pods ~5 GB (los 10,3 GB que marca `top nodes` incluyen caché del OS).
- **Basv2 no se pudo usar: cuota 0 para `standardBasv2Family` en Norway East.** La familia B v1 tenía 8 vCPU libres, así que se usó **B2ms** ($77/mes vs $131 del E2as_v4).
- Pool nuevo `b2mspool` (2 nodos, mode System, autoscaler min 2 / max 3, maxPods 250, k8s 1.35.7) creado de forma aditiva, sin tocar el viejo.
- Migración incremental: cordon de los 3 nodos viejos, drain de uno, verificación de que los pods arrancan en B2ms, y después los otros dos.
- Istio bloqueaba el drain: `istiod`, `istio-ingressgateway` y `istio-egressgateway` tenían 1 réplica con PDB `minAvailable: 1`. Se escalaron a 2, se drenó y se volvieron a dejar en 1.
- `e2asv4pool` eliminado. Estado final: 2 nodos Ready, todos los pods Running, 0 restarts, CPU requests 71–78 %, memoria 43–56 %.

### 3. MySQL staging apagada fuera de horario
Se verificó primero quién la usa: de noche, con el cluster QA apagado, `active_connections` = 4 y el `PROCESSLIST` mostró que **son las 4 conexiones internas de `azure_superuser`**, no usuarios. Ningún dev la usa desde local ni por túnel.
- Jobs nuevos en `cae-qa-ops`: **`job-qa-mysql-stop`** (`15 23 * * 1-5`) y **`job-qa-mysql-start`** (`55 5 * * 1-5`). La base arranca antes que el cluster (05:55 vs 06:00) y se apaga después (23:15 vs 23:00).
- Rol mínimo custom **`MySQL Start-Stop (vio-ecom-db-staging)`** (`read` + `start/action` + `stop/action`), con scope al servidor. Se creó primero un `Contributor` por error y se borró.
- `job-qa-mysql-start` probado en vivo: Succeeded, `accion=ninguna` con la base ya Ready.

### 4. `redus-vio-staging`: Balanced_B1 → Balanced_B0
Cambio de SKU **en caliente**, sin recrear (`az redisenterprise update --sku`). Estado final `Succeeded`. Dato útil: esto confirma que el SKU de Azure Managed Redis se puede bajar in-place, lo que aplica también a prod.

### Ahorro (medido, no estimado)
| Medida | Antes | Después | Ahorro/mes |
|---|---|---|---|
| PG backend prod apagada + guardián | $170 | ~$10 (solo storage) | ~$160 |
| Node pool QA 3×E2as_v4 → 2×B2ms | $265 | $132 | ~$133 |
| MySQL staging apagada 49 % de las horas | $137 | ~$90 | ~$47 |
| Redis staging B1 → B0 | $43 | $22 | ~$21 |
| **Total** | | | **~$361/mes** |

Run-rate: **$2.155 → ~$1.950/mes**, y además no se materializan los $170/mes que la PG acababa de empezar a facturar.

### Hallazgos colaterales
- **QA tenía pods roto desde hacía 9 h antes de esta migración** (`Init:Error`, `PodInitializing`: middleware, shopcart, tracking, users). Se limpiaron solos al rotar los pods. No eran consecuencia del cambio.
- **`api-staging-commerce.vio.live` y `api-qa-commerce.vio.live` no tienen registro DNS** (`dig` no devuelve nada). Sólo `graph-ql-staging.vio.live` resuelve, al ingress 20.251.70.230. Es el mismo hallazgo del audit de DNS de julio y sigue sin resolverse.
- Cuota: `standardBasv2Family` en Norway East está en 0. Si se quiere Basv2 (más barato que B v1) hay que pedir aumento de cuota.

### Pendiente de decisión de Angelo (prod, NO ejecutado)
1. AKS prod: bajar requests de 200m a ~50m y pasar a un pool más chico (~$230/mes). Uso real 0,64 cores y 14 GB sobre 12 vCPU y 48 GB.
2. Redis prod: hoy son 2 instancias B1 en Norway East por HA+ZR = $308/mes. Bajar a B0 manteniendo HA son ~$194/mes de ahorro **sin perder resiliencia** (y el cambio de SKU es en caliente, ya probado en staging). Quitar la HA serían ~$154/mes pero pierde el failover de zona.
3. MySQL prod D2ds_v4 → Burstable B2s (~$125/mes), requiere reinicio.

## Desmantelamiento de Vio Backend prod — Miguel
- Quién: Miguel, con OK explícito de Angelo ("desmantela prod de vio", 10:51). Alcance acordado: **Vio Backend prod**. Vio Commerce prod NO se tocó (3 clientes sincronizando).
- Dónde: RG `rg-api-vio-production` completo, más registros DNS en Cloudflare (zona vio.live).
- Cuándo: 2026-09-25 11:00–11:45
- Contexto: el entorno llevaba desde el 16/09 con las dos Container Apps en `Stopped`, `api.vio.live` y `events.vio.live` devolviendo 404, y nadie lo notó en 9 días. Staging (24/7) es el que sirve la demo, con 400–3.500 req/día reales. Angelo está evaluando mover backend a Oracle Cloud Always Free o Hetzner, así que migrar un entorno muerto no tenía sentido: se desmantela y, cuando haga falta, se levanta directo en la plataforma nueva.

### Respaldo antes de borrar (lo importante)
La base era diminuta: **~600 filas en total** (la tabla más grande, `shoppable_ad_activations`, 107 filas; 12 usuarios; 38 tablas), dump comprimido de **44 KB**, sobre 64 GB provisionados y 5,72 GB de storage. El storage estaba plano en 5,72 GB desde agosto: era todo overhead, no datos.

Como el servidor es privado (`publicNetworkAccess: Disabled`, subnet delegada), el dump se corrió como Container Apps Job **dentro del entorno prod**, que comparte VNet con la subnet de la PG — y por eso había que hacerlo antes de borrar el entorno.

**El primer dump no servía.** Se hizo con `postgres:17-alpine` contra un servidor 16.15 y falló al restaurar: pg_dump 17 emite `SET transaction_timeout = 0`, que no existe en PG 16. Pasó todas las validaciones de archivo (gzip íntegro, 39 tablas, 39 COPY, cierre correcto) y aun así era inservible. Se rehizo con `postgres:16-alpine`. Lección: `docs/lessons/pg-dump-version-debe-coincidir-con-el-servidor.md`.

Verificación final, restauración real antes de borrar: se bajó del blob, se restauró en una base nueva con `ON_ERROR_STOP=1` → OK, 38 tablas origen = 38 restauradas, y conteos idénticos (users 12, broadcasts 18, polls 39, chat_messages 37, cart_intents 85, shoppable_ad_activations 107).

Copias del respaldo:
- Azure blob `saapivio` (RG `rg-vio-shared`, intacto), container `db-snapshots`: `prod-decommission-2026-09-25-0856.sql.gz` + `.globals.sql`. Se **habilitó soft delete** (30 días, blobs y containers) en esa cuenta, que lo tenía desactivado.
- Local en la máquina de Miguel: `~/vio-backups/pg-api-vio-production-decommission-2026-09-25/` con README e instrucciones de restore.
- R2: **PENDIENTE**. El endpoint de R2 no es alcanzable desde esta máquina (handshake TLS falla, curl devuelve 000). Conviene subirlo desde otra máquina para tener una copia fuera de Azure.

### Verificaciones antes de borrar
- La VNet de prod **no tenía peerings** — aislada.
- La identidad `id-api-vio-production` sólo tenía `AcrPull` sobre `acrvioapi`, que vive en `rg-vio-shared` y no se toca: **las imágenes se conservan**.
- ClickHouse es **compartido con staging** (mismo host `vio-clickhouse.norwayeast.cloudapp.azure.com`, bases `vio_production` y staging): la VM `vm-clickhouse-vio` NO se borró. La base `vio_production` sigue ahí.
- CI/CD: `deploy.yml` de vio-backend despliega a **staging** en push a `main`; producción sólo por `workflow_dispatch` manual. Así que el desmantelamiento no rompe el pipeline automático. Un dispatch manual a production ahora fallará, que es lo correcto.
- No se encontró Terraform gestionando estos recursos.

### Ejecutado
1. Borrados los 4 registros DNS en Cloudflare **primero**, para no dejar ni una ventana con CNAMEs colgando a un hostname de Azure liberado (riesgo de subdomain takeover): `api.vio.live` (CNAME), `events.vio.live` (CNAME) y los dos TXT `asuid.*` de validación.
2. `az group delete rg-api-vio-production` — se llevó las 2 Container Apps, el entorno `cae-api-vio-production` (y su load balancer), `pg-api-vio-production`, la VNet, la zona DNS privada, el Log Analytics, la identidad y los jobs residuales (`db-restore-job`, `db-check-prod`, `db-check3`).
3. Borrado el guardián `job-pg-prod-stop-guard` que se había creado esta mañana, más su rol custom `PostgreSQL Stop Guard (pg-api-vio-production)`. La identidad `id-qa-aks-scheduler` queda sólo con sus 2 roles legítimos (AKS QA y MySQL staging).

### Ahorro
~$49/mes que se pagaban por un entorno apagado: $39 del load balancer del entorno de Container Apps (facturaba con las dos apps en `Stopped`) + ~$10 del storage de la PG. Si se hubiera encendido, eran ~$220/mes más.

**Run-rate acumulado del día: $2.155 → ~$1.900/mes.**

### Pendiente
- Subir el dump a R2 desde una máquina con salida a `*.r2.cloudflarestorage.com`.
- Angelo está evaluando Oracle Cloud Always Free vs Hetzner para rehospedar backend. Bloqueos conocidos: WebSockets necesitan proceso siempre encendido (descarta Vercel/Netlify y hace que Render free se duerma a los 15 min), y ClickHouse no tiene free tier managed serio — aunque Mixpanel ya está conectado en analytics y quizá lo reemplace.

## ClickHouse: la base de producción está vacía — Miguel
- Quién: Miguel (investigando el punto 2 del plan de Angelo: qué hacer con ClickHouse al rehospedar)
- Dónde: `vm-clickhouse-vio` (B2s, RG `rg-vio-shared`), `https://vio-clickhouse.norwayeast.cloudapp.azure.com`
- Cuándo: 2026-09-25 ~12:00

### Hallazgo
Conteos reales vía `system.parts` y `count()` directo:

| Base | Tablas | Filas | Tamaño |
|---|---|---|---|
| `vio_production` | 1 (`events`) | **0** | 0 |
| `vio_staging` | 1 (`events`) | 3.621 | 183 KiB |
| `vio_development` | — | 7 | 3,6 KiB |
| `system` (logging interno de ClickHouse) | — | **729.951.690** | **1,40 GiB** |

O sea: la VM dedicada + su disco cuestan **~$52/mes** ($41 VM + $11 disco) para sostener **una tabla
con 3.621 filas en staging y cero en producción**. El 99,99 % de lo almacenado es ClickHouse
registrándose a sí mismo, y las tablas `system` no tienen TTL configurado.

### Recomendación
**No migrar ClickHouse a Oracle: retirarlo.** 3.621 filas de eventos entran de sobra en la Postgres
que backend ya va a tener, y para analítica de producto **Mixpanel ya está conectado** en
`ca-analytics-vio-*` (`MIXPANEL_ENABLED`, project token y api secret presentes como secrets). Retirarlo
además saca un contenedor y su RAM de la caja de Oracle.

Antes de borrar: confirmar con Alan que no haya un consumidor que no se vea desde la infra (algún
dashboard o query manual). Con la tabla de producción en cero es poco probable, pero es destructivo y
el costo de preguntar es nulo.

**NO ejecutado** — espera OK de Angelo.

### Estado del plan de rehospedaje
Angelo descartó integrar backend al cluster de Commerce y eligió Oracle por simplicidad, con el
argumento de que un código que creció a base de "probemos esto" es justo lo que no conviene poner a
compartir cluster con los 3 clientes de Commerce. Razonable: aísla el desorden mientras se limpia.

Orden acordado: vio-backend, después ClickHouse, después la migración a Oracle. Bloqueado en que
Angelo genere la API key de OCI (se le pasaron los pasos y la policy mínima: usuario dedicado +
compartment `vio-backend` con `manage all-resources` ahí y `read all-resources` en la tenancy).
OCI CLI 3.94.0 ya instalado en la máquina de Miguel.

La medición del código de backend quedó en `docs/architecture/vio-backend-estado-y-limpieza.md`.
