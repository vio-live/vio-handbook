## Staging (kubernetesqa) no arranca — cuota de cores EASv5 en 0 — Miguel

- Quien: Miguel (agente infra). Reportado por Angelo ("staging esta arriba?").
- Donde: AKS `kubernetesqa` (RG `qa`, NorwayEast), nodepool `e2aspool` (Standard_E2as_v5 x2). Suscripcion `Microsoft Azure Sponsorship` (3d276f7e-0783-4581-8a49-ad0a2c432c63).
- Cuando: 2026-09-18, 09:55-10:30 CEST.
- Contexto: staging caido. Todos los endpoints de staging responden HTTP 000 (sin conexion).

### Causa raiz
La cuota `Standard EASv5 Family vCPUs` en NorwayEast esta en **limit 0** (uso 0/0). El nodepool necesita 4 cores. El VMSS `aks-e2aspool-29962209-vmss` quedo con capacity 0 y cada intento de `virtualMachineScaleSets/write` falla con:

```
OperationNotAllowed: exceeding approved standardEASv5Family Cores quota.
Location: NorwayEast, Current Limit: 0, Current Usage: 0, Additional Required: 4
```

Errores repetidos en el activity log desde 07:58 UTC. El cluster queda en `powerState=Running / provisioningState=Starting` sin nodos (`kubectl get nodes` -> No resources found).

Coincide con el fin de creditos de la suscripcion Sponsorship el 16/09: la cuota de esa familia se puso a 0.

### Causa secundaria (ya corregida)
El cron `qa-cluster-start` (08:00 L-V) reporto "ok" sin ejecutar nada. Corrio con el modelo de fallback `azure-openai/gpt-4o`, que no expuso la tool `exec` y devolvio "La herramienta az.aks.show no esta disponible". `toolsAllow` ya era correcto (`exec,read,write`); el problema era el modelo.
Fix aplicado: `openclaw cron edit <id> --model anthropic/claude-sonnet-5 --fallbacks google/gemini-3-6-flash --tools exec,read,write` en `qa-cluster-start` y `qa-cluster-stop`. Verificado.

### Cuotas verificadas (NorwayEast)
- Standard EASv5 Family vCPUs: **0/0** (staging, bloqueado)
- Standard DASv5 Family vCPUs: 0/22 (familia de prod `vio-commerce-prod`, D4as_v5 — sigue disponible)
- Total Regional vCPUs: 2/51
- Resto de familias: limite por defecto 10

`az quota update` a 10 cores fallo con `QuotaNotAvailableForResource`: en Sponsorship sin creditos no hay self-service, requiere ticket/reactivacion de billing.

### Hecho
- `az aks start` ejecutado manualmente: no sirve, falla por cuota.
- Provider `Microsoft.Quota` registrado en la suscripcion (era necesario para consultar/pedir cuota).
- Crons de encendido/apagado de QA pinneados a modelo fiable.

### Pendiente (decision de Angelo)
1. Reactivar creditos/billing de la suscripcion -> recupera la cuota EASv5.
2. O migrar el nodepool de staging a familia DASv5 (ej. D2as_v5), que si tiene cuota. Implica crear nodepool nuevo y borrar `e2aspool`; baja RAM de 16GB a 8GB por nodo, al reves del cambio a E-series que se hizo por workloads Node.js con mucha memoria.
3. Mientras tanto el cluster queda en `Starting` reintentando y fallando; y el cron de las 08:00 va a seguir fallando cada dia habil.

## Investigacion de billing — el billing NO esta roto, es re-baseline de cuotas — Miguel

- Quien: Miguel. Pedido por Angelo ("quiero arreglar el billing").
- Cuando: 2026-09-18, 10:35-10:50 CEST.

### Estado real del billing (verificado via ARM/Billing API)
- Suscripcion `3d276f7e-0783-4581-8a49-ad0a2c432c63` "Microsoft Azure Sponsorship": `state=Enabled`, `quotaId=PayAsYouGo_2014-09-01`, `spendingLimit=Off`. Ya convertida a pago por uso, sin limite de gasto.
- Billing account `fe07e8ba-42cb-40e6-87a4-8845ac762f28` (MicrosoftOnlineServicesProgram, legacy PAYG): `accountStatus=Active`. Contiene 4 suscripciones Sponsorship, todas `Active`.
- Segunda billing account `26307a5a-...:968ba9d3-..._2019-05-31` (MicrosoftCustomerAgreement, "Angelo Sepulveda"): Active, contiene "Azure subscription 1" (`a58c007a-47b2-4465-bb60-576e19bdf16d`).

**Conclusion: no hay nada suspendido ni impago que arreglar por API.** Lo que paso al terminarse la sponsorship es que Azure re-baseliniza las cuotas de compute.

### El re-baseline afecta a TODA la generacion v5 de la familia E
Cuotas en NorwayEast:
- En 0: EASv5, EADSv5, EDSv5, ESv5, Ev5, EPSv5, EPDSv5, EIxxv5
- En 10: EBSv5, EBDSv5, EASv4, EDSv4, ESv3, Ev3, Ev4, Easv7, Eadsv7, Edsv6, Esv6, Eav6, Eadv6
- DASv5 (familia de prod, D4as_v5): 22

### Peticiones de cuota: rechazo automatico confirmado
El `az quota update` inicial fallaba por el parametro `--resource-type` del CLI, no por billing. Via REST directo si entra la peticion, pero Azure la rechaza:
- `PUT .../quotas/standardEASv5Family` value=10 -> request `33f924e1-aab9-4387-83d5-b2c54d82a0a4` -> `Failed: QuotaNotAvailableForResource`
- Reintento con value=4 (lo minimo necesario) -> request `13880775-f09e-4425-86ec-34b76938c3c9` -> `Failed: QuotaNotAvailableForResource`

El SKU `Standard_E2as_v5` NO tiene restricciones para la suscripcion (`az vm list-skus` -> sin restricciones), asi que no es inelegibilidad. Unico camino restante para EASv5: ticket de soporte con revision manual.

### Alternativas de SKU que ya tienen cuota (precios retail NorwayEast, Linux consumption)
| SKU | vCPU | RAM | USD/h | ~USD/mes (2 nodos, 365h) | Cuota |
|---|---|---|---|---|---|
| Standard_E2as_v5 (actual) | 2 | 16 GiB | 0.1620 | ~118 | **0 — bloqueado** |
| Standard_E2as_v4 | 2 | 16 GiB | 0.1800 | ~131 | 10 |
| Standard_E2bs_v5 | 2 | 16 GiB | 0.2130 | ~155 | 10 |
| Standard_D2as_v5 | 2 | 8 GiB | 0.1230 | ~90 | 22 (compartida con prod) |

### Huella real de staging (medida sobre los deployments, API server vivo sin nodos)
29 deployments, requests totales **3.0 GiB RAM / 1.5 vCPU**. DaemonSets ~450Mi por nodo Linux. Total ~4 GiB contra 32 GiB provisionados.

### Recomendacion
Migrar el nodepool a **Standard_E2as_v4**: mismos 2 vCPU / 16 GiB que hoy, cuota ya disponible, no revierte la decision de sizing E-series, +~13 USD/mes. `D2as_v5` queda como opcion si se prioriza ahorro (~28 USD/mes menos que hoy), justificable por la huella real de 4 GiB.

### Pendiente de decision de Angelo
- Que SKU usar para el nodepool nuevo.
- Si se abre ticket de soporte a Microsoft para recuperar cuota EASv5 (accion externa, no ejecutada).
