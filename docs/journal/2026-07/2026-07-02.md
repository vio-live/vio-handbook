# Journal 2026-07-02 — Miguel

## Audit de infra + incorporación de Alan al canal #infra-vio

### Contexto
Angelo quiere que Miguel sea la fuente de verdad de infra para él y para Alan (dev del equipo). Incorporamos a Alan al canal `#infra-vio` (ID `1521869222181867623`) en Discord.

---

## Audit Azure — hallazgos verificados en live

### ✅ OK
- **Cluster activo:** `vio-commerce-prod` (RG `rg-vio-commerce-prod`) — 3 nodos Standard_D4as_v5, k8s v1.34.8, Istio ingressgateway IP `20.100.174.93`
- **Redis Enterprise:** `redus-vio-prod` y `redus-vio-staging` en `rg-vio-databases` — Running, TLS (`Encrypted`), puerto 10000, Redis 7.4, host `redus-vio-prod.norwayeast.redis.azure.net`
- **MySQL:** `vio-ecom-db-prod` (Standard_D2ds_v4) y `vio-ecom-db-staging` en `rg-vio-databases`
- **Container Apps socket-server:** prod/staging/dev — todos `Succeeded`
- **DNS activos:** `api-ecom.vio.live`, `graph-ql.vio.live`, `dashboard.ecom.vio.live` → `20.100.174.93` ✅
- **DNS backend:** `api.vio.live`, `api-staging.vio.live`, `api-dev.vio.live` → Container Apps ✅
- **16 microservicios** corriendo en namespace `default` del cluster

### 🔴 Problemas / drift detectados

1. **`kubernetesqa` sigue corriendo en Azure** (RG `qa`, `provisioningState: Succeeded`) — Angelo lo da por eliminado pero está vivo y consumiendo. Está en TF state como `azurerm_kubernetes_cluster.qa`. **Pendiente destruir.**

2. **Redis Enterprise NO está en Terraform** — `redus-vio-prod` y `redus-vio-staging` creados manualmente por Alan el 2026-07-01. Drift completo. **Pendiente importar al state.**

3. **MySQL drift de SKU** — TF dice `B_Standard_B2s`, Azure muestra `Standard_D2ds_v4`. Investigar.

4. **DNS faltante:** `sales-channel.vio.live`, `shopify-seller.vio.live`, `msrvc.vio.live` — sin registro DNS. Los ingresses existen pero no llegan tráfico externo.

5. **`reachu-prod` cluster eliminado** pero su RG `prod-reachu` sigue vivo (ACR `reachuprod2`, Service Bus, storage). TF state aún lo referencia. **Pendiente limpiar.**

6. **Dominios legacy:** `api.reachu.io` y `graph-ql.reachu.io` apuntan a IPs no identificadas. Zona reachu.io no bajo nuestro control directo.

---

## Cambios realizados

- **`azure-overview.md` actualizado** con estado real: `vio-commerce-prod` como único cluster, Redis Enterprise, eliminación de reachu-prod y kubernetesqa. Commit `50a47de` en vio-handbook main.
- **Alan incorporado a `#infra-vio`** — Miguel presentado como fuente de verdad de infra con control operativo sobre `vio-commerce-prod`, Cloudflare y Azure.
- **Cron semanal creado** — cada lunes 03:00 Oslo: audit automático + update handbook + reporte en `#infra-vio`.

---

## Pendiente

- [ ] Destruir `kubernetesqa` via Terraform
- [ ] Importar `redus-vio-prod` y `redus-vio-staging` al state de TF
- [ ] Investigar drift de SKU MySQL
- [ ] Crear DNS para `sales-channel.vio.live`, `shopify-seller.vio.live`, `msrvc.vio.live`
- [ ] Limpiar RG `prod-reachu` (o decidir qué conservar)
