# Informe Detallado — Sesión 27 de Mayo, 2026 ⚙️

---

## **Tareas Ejecutadas y Cambios Realizados:**

### 1️⃣ **Estado de los Clústeres Kubernetes:**
#### *`kubernetesqa` (Entorno de QA):*
- **Estado revisado:** Todos los pods se encuentran en estado `Running`, indicando estabilidad operativa.
- **Configuración de Istio:**
  - Istio configurado con un **Ingress Gateway** funcional.
  - Herramientas de monitoreo activas:
    - **Kiali:** Observabilidad visual de tráfico de servicios.
    - **Jaeger:** Sistema de trazabilidad distribuida.
    - **Prometheus:** Recolección de métricas de rendimiento.
- **Servicios principales:**
  - **APIs:** Con servicios REST/GraphQL (`api-dev.vio.live` y `graph-ql-dev.vio.live`).
  - Aplicaciones funcionando sin interrupciones.
- **Redis:**
  - Persistencia habilitada en modo mixto (**Snapshots + AOF**):
    - **Configuración:** Snapshots cada 900s o 10 cambios mínimos, AOF continuo habilitado.

#### *`reachu-prod` (Producción):*
- **Estado general:** Todos los pods en `Running` y Istio funcionando correctamente.
- **Certificación TLS:** Configurada con Cert-Manager para dominios públicos como `api.reachu.io`.
- **Servicios principales:** APIs de producción y GraphQL (`api.reachu.io`, `graph-ql.reachu.io`).

---

### 2️⃣ **Mejoras en Redis:**
- **Problema detectado:** Durante las revisiones, descubrimos que el dominio DNS `redis.default.svc.cluster.local` no se resolvía correctamente.
- **Causa:** El servicio `redis` está configurado con un `ClusterIP` como `None`. Esto limita accesibilidad desde consultas basadas en DNS.
- **Solución pendiente:**
  - Discutir con Alan si se modifica la configuración del ClusterIP o se prefiere usar IPs directas.

---

### 3️⃣ **Diagnósticos de Queries Lentas e Índices:**
- **Tarea inicial:** Desarrollamos un script para diagnosticar queries lentas y revisar índices en bases de datos PostgreSQL/MySQL.
- **Restringido:** Descubrimos que la base de datos está alojada **fuera de Azure**.
  - **Acción pendiente:** Obtener credenciales de acceso remoto para implementar diagnósticos reales y completarlos.

---

### 4️⃣ **Sistema de Monitoreo Continuo:**
- **Configuración realizada:**
  - Implementamos un script para monitorear:
    - Tiempos de respuesta en APIs (`https://api.reachu.io`).
    - Estado de Redis, con detalles de latencia y recursos.
  - Configuramos un cron job para ejecutarlo cada 6 horas.
- **Resultados actuales:**
  - **APIs:** Latencia promedio baja (~0.1s). Respuesta HTTP 200 correcta.
  - **Redis:** Presentó errores de acceso debido al problema DNS mencionado.

---

## **Tareas Pendientes / Puntos para Revisar:**

### 🔹 *Sobre Redis:*
1. Resolver si queremos modificar el servicio ClusterIP actual (`None`) o utilizar una alternativa con DNS funcional.
2. Revisar configuración de Istio para evitar posibles bloqueos de tráfico en Redis (puerto 6379).

### 🔹 *Diagnósticos de Base de Datos Externa:*
1. Obtener acceso remoto (host, credenciales, etc.).
2. Ajustar scripts para interactuar con la instancia correctamente.

### 🔹 *Infraestructura y Documentación Detallada:*
1. Crear diagramas detallados de flujos API → Redis → Istio.
2. Usar herramientas como Azure Resource Visualizer o Visio (en discusión pendiente).

El contenido ha sido adaptado para que cualquier persona involucrada en el proyecto pueda entender claramente:
1. Qué se realizó.
2. Qué ajustes se aplicaron.
3. Qué tareas quedan pendientes.

---