---
date: 2026-05-07
session: full-day
participants: [angelo, claude]
status: live
---

# Session — 2026-05-07 — VG advertorial demo + handbook bootstrap

## Goal

Build the VG advertorial demo end-to-end (live VG news feed + Maxbo article + product carousels + Apple Pay) for a partner pitch. Then leave the project in a state where any next session — human or AI — can resume without context loss.

## Done

### VG demo shipped (PR #14, commit `e463c51` on develop)

- Live VG news feed: `NewsView` + `VGNewsFeedViewModel` + `VGRSSService` parsing `https://www.vg.no/rss/feed/?format=rss` with kicker auto-split, RFC 822 dates, image fallback chain.
- Maxbo advertorial: `MaxboArticleView` with sticky topbar (Ferdig + ANNONSØRINNHOLD + sponsor logo from `VioConfiguration`), full-bleed hero with overlay headline, 3× `VProductCarousel` showing 4 Weber products from campaign 38, `VProductStore(locationId: "home_store")` at the tail, Schibsted Partnerstudio footer.
- Two-tone burgundy palette (`pageBackground #1C0000` for page + topbar, `burgundy #320000` for cards) so card edges are visible.
- VG SVG wordmark in Assets (vector representation preserved).
- iOS config: api key flipped to `vg_api_key_05e51473f1704187` (campaign 38), `theme.mode = "light"`, `priceColor = "#E61A22"`. Apple Pay capability with `merchant.live.vio` + `merchant.vio.development`. `CODE_SIGN_ALLOW_ENTITLEMENTS_MODIFICATION = YES` (was missing — TV2 had it). `userId = "demo_user_001"` + `VgPlacementRegistration.registerAll()` mirroring TV2's working setup.
- SDK polish (cross-host benefit): `VProductDetailOverlay` frosted-glass bottom action bar + image margins + theme-pinned scheme. `VProductCard` + `VProductSpotlight` `.fit + .sm` inset. `VApplePayButton` purple→black. `VApplePayConfirmationSheet` full theme refactor from hardcoded TV2 colors to `VioColors.adaptive(for:)` tokens. `VProductCarousel` defensive sponsor fallback. `PaymentRuntimeGuard` skip-if-bootstrapped (avoids iOS 26 deadlock at `await MainActor.run`).

### Backend bookkeeping shipped (PR #36, commit `968374d` on develop)

- `client/src/pages/sponsors.tsx` — PATCH body now includes `userId` (was 400).
- `server/routes.ts` — `GET /api/campaigns/broadcast-counts?ids=…` added before the `:id` catchall (was 500 from NaN parseInt).
- `scripts/inspect-vg-campaign.ts` — read-only diagnostic for campaign 38.
- `docs/CURRENT_STATE.md` §26 added; header timestamp refreshed.

### vio-handbook repo bootstrapped (commit `90fda5b` on main)

- Private repo `vio-live/vio-handbook` created.
- 14 files: README hub, onboarding (humans + agents), architecture/system-overview, 5 ADRs, 1 sprint summary (VG), 2 lessons (iOS 26 modal hang + Stripe Connect per sponsor), 1 playbook (debug Apple Pay), glossary, roadmap stub.
- Handbook = cross-repo durable knowledge. Code-coupled docs stay in their respective repos.

### Memory + Q4 L4 handoff updated

- `MEMORY.md` index now points to handbook as first read.
- `project_features_registry.md` adds "Cross-repo handbook" section at top.
- `project_pending_critical.md` reflects VG closed + Q4 L4 stuck with refresh notes + new cart overlay bug surfaced today.
- `Q4-L4-HANDOFF.md` (in feat branch) refreshed with develop drift table + resume sequence.
- `CURRENT_STATE.md` §26 + header dual-status (VG done, Q4 L4 stuck).

## Decisions

- [ADR-0003: Don't nest .fullScreenCover with .sheet for async-payment flows on iOS 26](../../decisions/0003-no-fullscreencover-with-sheet-on-ios26.md)
- [ADR-0004: Host themes via vio-config.json](../../decisions/0004-host-themes-via-vio-config.md)
- [ADR-0005: Placement-level styling belongs in custom_config (draft)](../../decisions/0005-placement-styling-belongs-in-custom-config.md)
- ADR-0001 + ADR-0002 backfilled (operating rules that pre-existed but weren't formalized as ADRs).

## Lessons banked

- [iOS 26 nested modal Task-resume deadlock](../../lessons/ios26-nested-modal-deadlock.md) — burned ~5 hours diagnosing. Bisected via TV2 (works) vs Vg (hangs) with same SDK code path. Fix: in-place tab swap instead of nested modals.
- [Stripe Connect + merchant.live.vio per sponsor](../../lessons/stripe-connect-per-sponsor.md) — Maxbo (sponsor 8) was correctly set up; included status table for all sponsors.

## Blockers / open questions

- **Cart overlay multi-sponsor bug**: surfaced near sprint close. `VFloatingCartIndicator` reads `cartManager.itemCount` (legacy `items`); when products go to `cartsBySponsor[8]`, indicator stays empty. Diagnosed but not fixed — workaround documented. Tracked in `CURRENT_STATE.md §26 Known issue`.
- **Placement-level styling backend** (ADR-0005, draft): white wrapper around `VProductStore` is host-side. Backend epic to add `backgroundColor` + padding to `app_placements.custom_config`.

## Next session

- **Likely**: resume Q4 L4 from `feat/multi-sponsor-checkout-flow`. Read order:
  1. `vio-handbook/docs/onboarding/agents.md`
  2. `socket-server/docs/CURRENT_STATE.md` §25
  3. `VioSwiftSDK/Q4-L4-HANDOFF.md` (refreshed today)
- **Possibly**: knock out the cart overlay multi-sponsor bug first (smaller, blocks UX in Vg).
- **Possibly**: kick off ADR-0005 backend implementation (placement styling).

## Reference

- VG PR: [vio-live/VioSwiftSDK#14](https://github.com/vio-live/VioSwiftSDK/pull/14)
- Backend PR: [tipiodevelopment/socket-server#36](https://github.com/tipiodevelopment/socket-server/pull/36)
- Handbook commit: `90fda5b` on `main` of `vio-live/vio-handbook`
- Sprint retrospective: [`sprints/2026-05-vg-advertorial-demo.md`](../../sprints/2026-05-vg-advertorial-demo.md)
