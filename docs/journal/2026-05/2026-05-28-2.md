---
date: 2026-05-28
session: full-day
participants: [angelo, claude]
status: live
---

# Session — 2026-05-28 — Adtraction pilot prep + Vev iOS cards (Alan) + Neon cost analysis

> Companion del entry de Miguel del mismo día (`2026-05-28.md`, debugging api-commerce). Este es el track de Angelo ↔ claude.
> Autoría: dirigido por angelo, ejecutado por claude (research Adtraction, Trello cards, análisis Neon, regla de autoría).

## Goal

Avanzar el partnership con Adtraction (post-acceso a su plataforma), spec'ear el experiment VG × Vev para Alan, y diagnosticar el costo de Neon que preocupaba a Angelo.

## Done

### Adtraction — exploración de API + arquitectura del piloto

- Angelo consiguió acceso como **publisher** a Adtraction + **3 marcas para testear** (Lyko NO está entre ellas — la decisión de merchant aún no se tomó; es fase de descubrimiento técnico, no commit comercial).
- Exploré la API (v2 y v3 vía apidocs.adtraction.net). Hallazgo contraintuitivo: **v3 es MÁS acotada que v2** para nuestro caso (sin tracking-link generation, sin transactions endpoint, sin `epi1–5` custom — solo `setEpi` boolean).
- **Gap crítico**: ni v2 ni v3 documentan un **S2S postback** para reportar conversiones in-app. Es el nodo que sostiene la viabilidad comercial (sin él Allermedia no cobra). Casi seguro existe pero configurado dashboard-side o en un developer kit no público.
- Armé doc con 3 diagramas Mermaid (arquitectura / transaksjonsflyt / provisjons-flyt) en `meetings/2026-05-24-adtraction-pilot-arkitektur.md` — **dejado UNTRACKED a pedido de Angelo** (local-only por ahora).
- Redacté email a Adtraction (noruego): 2 preguntas técnicas (S2S postback + EPI propagation) + ejemplo concreto del payload + cierre pidiendo reunión conjunta con Allermedia. Respondí su follow-up sobre el `click_id` (confirmado: es el click en la flate de Aller que abre el checkout in-app).

### Trello — 5 cards para Alan (VG × Vev, iOS)

En `To do`, asignadas a Alan, prioridad **ALTA**. Scope: convertir el artículo nativo de VG a uno Vev-renderizado en WKWebView + checkout nativo. **Adtraction excluido (phase 2)** — primero hacer funcionar el flow con lo nuestro.

1. [Vev plugin — auditar + actualizar a Vio Commerce API](https://trello.com/c/SUG6Z50H)
2. [Vev plugin — generar deep link Vio al insertar producto](https://trello.com/c/mx80SLEA)
3. [VG iOS demo — VevArticleView con WKWebView wrapper](https://trello.com/c/Cy8rWdVj)
4. [VevArticleView — intercepción WKNavigationDelegate + handoff a VCheckoutOverlay](https://trello.com/c/LnQSQJfH)
5. [VG × Vev — test end-to-end + journal entry](https://trello.com/c/gZyiqtE7)

Verifiqué que `develop` de VioSwiftSDK está limpio para que Alan se suba (`MaxboArticleView` + `VGHomeView` + `VCheckoutOverlay` presentes, sin colisión con `feature/vg-vev-article`).

### Neon — análisis de costo (`docs/infrastructure/neon-cost-analysis.md`, commit `25ab75b`)

Angelo preguntó si conviene sacar Neon por costo. Diagnóstico vía Neon API (key project-scoped a `silent-recipe-32135926`):
- Costo = **compute (213 CU-h/mes), NO storage (0.12 GB) ni cantidad de branches**.
- Driver = `develop` corriendo ~24/7 (api-dev + backends locales nunca lo dejan suspender).
- Sacar Neon es viable pero ahorra ~$6/mes a esta escala → no vale por este project solo.
- Palanca real: **suspender `develop` off-hours** (gratis, ~40% menos compute).

### Handbook — regla de autoría en journals

Codificada en `docs/journal/README.md` (commit `cad0bf5`): cada entry declara autor en frontmatter (`author:` / `participants:`), incluyendo agentes AI, para trazar quién hizo qué. **Commits de git quedan limpios** (sin atribución AI — regla locked nº6 intacta).

## Decisions

- **Adtraction phase 1 = sin Adtraction**: validar el flow Vev + checkout nativo primero; tracking/postback de Adtraction como phase 2 cuando confirmen el S2S.
- **Apple Pay desde Vev**: el WKWebView solo muestra el artículo; el checkout (Apple Pay) se dispara nativo tras interceptar el tap. Nunca se usa Apple Pay JS API (evita registro de dominio del publisher).
- **No rip-out de Neon** por este project — optimizar (suspend off-hours) en vez de migrar.

## Blockers / open questions

- **Adtraction S2S postback** — bloqueante de viabilidad comercial. Email enviado, esperando respuesta.
- **¿Más projects Neon en la org?** — para Miguel. La API key es project-scoped, no ve otros; cambia todo el cálculo de costo. (Nota: en el rebase de hoy entraron commits de Miguel removiendo PostgreSQL de `main.tf` en Terraform — puede estar relacionado.)
- **Plugin Vev** — Alan tiene que localizarlo. No aparece en las 3 orgs GitHub accesibles (`vio-live`, `ReachuDevteam`, `tipiodevelopment`) vía `gh`; Angelo confirma que existe.
- **Cleanup SDK** (parqueado, NO ejecutado): 3 archivos stale en VioSwiftSDK develop —
  - `DEV_SESSION_2026-03-30.md` (scratchpad Amy↔Cursor)
  - `Demo/ReachuDemoApp/` (huérfano del rebrand, no compila)
  - `.vscode/launch.json` (paths `ReachuSwiftSDK` rotos)
  - + 5 `theme-examples/*.json` con placeholder `your-reachu-api-key-here`

## Next session

- Si Adtraction responde el email → crear las 3 cards de phase 2 (tracking link en plugin, `click_id` extraction en intercept, S2S postback en backend).
- Si Alan arranca las cards → review siguiendo `playbooks/review-kotlin-ios-parity.md`.
- Decidir si se ejecuta el cleanup de los 3-4 archivos stale del SDK.
- Miguel: leer `neon-cost-analysis.md`, verificar otros projects Neon, evaluar suspend off-hours de `develop`.

## Reference

- Trello board Dev: 5 cards en To do (links arriba).
- Neon analysis: `docs/infrastructure/neon-cost-analysis.md`.
- Adtraction arquitectura (untracked, local): `meetings/2026-05-24-adtraction-pilot-arkitektur.md`.
- Adtraction briefing previo: `meetings/2026-05-19-adtraction-briefing.md`.
