---
date: 2026-05-07
session: kotlin-review
participants: [angelo, claude]
status: live
---

# Session — 2026-05-07 — Kotlin/TV review (post-VG demo)

> Companion entry to [`2026-05-07.md`](./2026-05-07.md) which covers the VG advertorial demo (iOS). This file is the Kotlin/TV review track for the same day.

## Anchors before review

- VioKotlinSDK: `6fb599e2` (2026-05-05 23:43)
- VioKotlinDemo: `0c1b3a8` (2026-05-05 23:32)
- AndroidTV-Vio: `fc41d5b` (2026-04-30 21:53)
- AndroidTV-Vio-Demo: `5560efb` (2026-04-30 21:54)

## Track A — Trello state (last 24h on board "Dev")

**Alan: 10 actions**, all between 2026-05-05 13:22–13:23 (before his evening commits). Moved 5 cards `To do → Doing`:

| Card | Movement |
|---|---|
| M8 — VioPlacementRegistry | To do → Doing |
| M9 — CampaignManager fetch components dedupe | To do → Doing |
| M10 — BroadcastCapabilities | To do → Doing |
| M12 — Confirmation sheet sponsor logo | To do → Doing |
| TV8 — VioCommerceService.fetchProduct apiKey per-call | To do → Doing |

**Angelo (me): 0 actions** in last 24h.

Board state at review time:

| List | Kotlin/TV count |
|---|---|
| Done | 17 |
| Doing | 6 (M11 pre-existing + the 5 Alan moved) |
| To do | 8 (TV11 + Q4-K0 overview + Q4-K1..K6) |
| Backlog | 14 (Sprint S1–S14) |

## Track B — Alan's commits (verified in code)

**Zero new commits in last 24h.** Last activity remains the 5-may evening burst:

- VioKotlinSDK: 3 commits (`dbae6b09` 22:55, `f6b30201` 23:32, `6fb599e2` 23:43).
- VioKotlinDemo: 2 commits (`58c45a0` 22:55, `0c1b3a8` 23:32).
- AndroidTV-Vio: untouched since 30-apr.
- AndroidTV-Vio-Demo: untouched since 30-apr.

### Verified status of the 5 cards Alan claimed in his daily

Cross-checked the code against the daily message. Used [`lessons/verify-alan-claims-against-code.md`](../../lessons/verify-alan-claims-against-code.md) — three of five didn't match.

| Card | Verified state | Evidence |
|---|---|---|
| **M8** VioPlacementRegistry | ✅ DONE | `library/src/main/kotlin/live/vio/VioCore/placements/VioPlacementRegistry.kt` (49 lines), `VioPlacementComponent.kt` (19), `VioPlacementManifestUploader.kt` (85, posts `/v2/mobile/components/manifest`). Exposed in `VioCore.kt:35,39`. Demo `ViaplayPlacementRegistration` registers 5 locations. Commit `dbae6b09`. |
| **M9** fetch components + dedupe | ✅ DONE | `CampaignManager.fetchAndApplyCampaignComponentsIfPossible()` line 568+, GET `/v2/mobile/campaigns/${campaign.id}/components` line 586, dedupe `(id, locationId)` composite line 622-625. Called after manifest upload at line 458. |
| **M10** BroadcastCapabilities | ❌ UNTOUCHED | Zero matches for `BroadcastCapabilities`, `/v2/mobile/broadcasts/.*capabilities`, `currentCapabilities`, or any plausible synonym (`capability`, `featureFlag`, `hasShoppable`, etc). |
| **M12** Confirmation sheet sponsor logo | 🟡 PARTIAL — different scope from card | Card asks for confirmation sheet (Klarna/Stripe/Vipps success). Alan instead propagated `sponsorLogoUrl` in placement composables (`VioProductCarousel:109`, `VioProductSpotlight:81,90`, `VioProductStore:115`, `VioOfferBannerDynamic:105`). Real card scope (the payment confirmation sheet) untouched — `VioCheckoutOverlay` not modified in any commit. |
| **TV8** VioCommerceService.fetchProduct apiKey per-call | ❌ UNTOUCHED | `AndroidTV-Vio` has zero commits since 30-apr. `VioCommerceService.kt:21` still has the stub `"no implementado en este cut (usar embedded payload)"`. Mobile `VioCommerceService.kt` also unchanged in these commits. |

### Bonus work without a card

`registerComponentSponsor(sponsorId, sponsorConfig, commerceConfig)` added to `VioConfiguration.kt:226-262` (commit `f6b30201`). Dynamically registers a secondary sponsor that arrives via component (not in the bootstrap response). `Component.kt:152-241` and `ComponentResponse` got new fields `sponsorId/sponsor/commerce`.

This is precursor plumbing for the Q4 multi-sponsor cart sprint. No card associated.

## Track C — Our changes (Angelo + agent)

This session: **0 changes to Trello yet**. The user pointed out (correctly) that the agent had been reviewing Trello surface-level for 3 days without inspecting code. The deeper code review above was the corrective.

Pending decisions (waiting on user OK):

- Move M8 + M9 to Done with SHA citation.
- Comment on M10 asking Alan for clarification (claimed but no code).
- Comment on M12 explaining placement composables don't equal confirmation sheet — split or close?
- Comment on TV8 asking which repo he meant (AndroidTV-Vio is untouched; mobile already had `sponsorId` from M4).
- Decide whether to retroactively create a Done card for `registerComponentSponsor` bonus work.

## Process changes locked this session

- Added rule to `~/.claude/CLAUDE.md` (agent's per-instance memory): always fetch + diff + verify code before reporting Alan's progress. Three tracks (Trello state / Alan's commits / our changes) reported separately.
- Migrated the rule into the durable handbook as a playbook: [`playbooks/review-kotlin-ios-parity.md`](../../playbooks/review-kotlin-ios-parity.md).
- Banked the burn pattern as a lesson: [`lessons/verify-alan-claims-against-code.md`](../../lessons/verify-alan-claims-against-code.md).
- Agent scope formally bounded by the user: **iOS↔Kotlin parity, Alan's tasks, and journal entries here**. Backend reviews and direct iOS work are out of scope.

## Anchors after review (carry forward)

Same as before — Alan made no commits today. The next review's `git log <anchor>..HEAD` should still start from these:

- VioKotlinSDK: `6fb599e2`
- VioKotlinDemo: `0c1b3a8`
- AndroidTV-Vio: `fc41d5b`
- AndroidTV-Vio-Demo: `5560efb`

## Open questions for next session

1. M8 + M9 move to Done now or wait for Alan to close them himself?
2. M10 / M12 / TV8 comment text — let the agent draft and post, or human-write?
3. Bonus `registerComponentSponsor` — Done card with retroactive description, or leave as undocumented prep work?
4. iOS `develop` shipped Q4 L4 cart-per-sponsor enhancements (per the main 2026-05-07 journal) — should the existing Q4-K1..K6 cards in Trello be cross-checked against what iOS actually merged?
