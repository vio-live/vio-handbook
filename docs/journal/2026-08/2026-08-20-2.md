---
date: 2026-08-20
session: vio-analytics-v1-implementation (continuación — "ve con todo")
participants: [angelo, claude]
status: f3-f6-built, deploy-vev-pendiente-ok, provisioning-pendiente
---

# Session — 2026-08-20 (cont.) — merges, F3, F6 v1, Terraform validado

Continuación de [`2026-08-20.md`](./2026-08-20.md). Angelo pidió mergear los 3
PRs y "ve con todo".

## Hecho

1. **Merges (a pedido de Angelo)**: vio-backend#43 (F4) → main `f5fdfa1`,
   deploy a development EN VERDE (mirror off hasta configurar
   `ANALYTICS_EVENTS_URL`/`ANALYTICS_INTERNAL_TOKEN` en el Container App);
   vio-web-sdk#18 (F2) → `75dc59a` (sin publicar a npm aún); handbook#7.
2. **Ruido de deploys arreglado**: el workflow de deploy de vio-analytics
   fallaba en cada push (secrets Azure inexistentes) → **manual-only** con
   instrucciones para restaurar el trigger tras el provisioning.
3. **F3 — Vev consume el core** (PRs abiertos, mergear EN ORDEN):
   [vio-web-sdk#19](https://github.com/vio-live/vio-web-sdk/pull/19)
   (`autoTrack:false` — el motor sin auto-listeners) primero, luego
   [vev#7](https://github.com/vio-live/vev/pull/7) (trackEvent() forwarda al
   colector; GA4/inspector intactos; toggle "Send to Vio Analytics";
   rebundle incluido; `npx vev build` OK). **SIN `vev deploy`** — requiere
   OK explícito + verificación de timeline (regla 18-ago); sandbox personal
   disponible para probar.
4. **F6 v1 — API de agregados** (`vio-analytics` main `ce7c544`):
   `GET /v1/stats/{overview, campaigns/:id/funnel, components}` — scope
   estructural por api key (aislamiento cross-tenant verificado e2e), path
   de token interno para que los backends de los dashboards nombren tenant
   tras su propia authz (ADR-0008), lecturas con FINAL, 43 tests.
5. **Terraform validado** (OpenTofu 1.12.6, `tofu validate` verde) + lock
   file de providers commiteado. El `apply` sigue siendo de Angelo.

## Pendiente inmediato

- Angelo: mergear #19 → #7 · OK para `vev deploy` (o sandbox) · provisioning
  (sin cambios: ClickHouse Cloud EU, Mixpanel token+secret+región,
  `terraform apply`, secrets GitHub, CNAMEs) · env vars del mirror en
  `ca-api-vio-*` · decidir npm publish del web-sdk (0.4.2→0.5.0).
- Siguiente construcción: F5 (Swift/Kotlin) y consumo de /v1/stats desde
  los dashboards.

## Addendum — F5 (paridad nativa) + prueba local completa

6. **Prueba local del servicio entero** (a pedido de Angelo): backend real
   local (:5010, espejo activado) disparó un shoppable ad real por el
   endpoint TV (activación con producto del commerce), cart-intent de TV,
   sesión web con el SDK compilado → funnel completo en `/v1/stats`
   (impresiones, intents, purchases, GMV) con las dos mitades unidas por
   `activation_id`. Dos hallazgos: un dev server huérfano del clon viejo
   (`~/Documents/GitHub/socket-server`) robaba filas del outbox con código
   pre-F4 (muerto; lección: instancias viejas contra la misma DB roban
   outbox), y el dispatch del espejo trataba 202-con-rechazo como éxito →
   [vio-backend#44](https://github.com/tipiodevelopment/vio-backend/pull/44)
   (exige `accepted===1`).
7. **F5 — paridad nativa** (PRs abiertos, sin merge):
   [VioSwiftSDK#15](https://github.com/vio-live/VioSwiftSDK/pull/15) (base
   `develop`) y [VioKotlinSDK#1](https://github.com/vio-live/VioKotlinSDK/pull/1).
   `VioAnalyticsClient` espejo en ambos (contrato v1: anon/sesión
   persistentes, batch 20/5s, cola offline, retries con ids estables,
   surface ios/tvos/android/androidtv); `AnalyticsManager` conserva su API
   (los componentes UI instrumentados no cambian) y forwarda los eventos
   mapeados; path Mixpanel legacy intacto (deprecar después); guard de PII
   (emails del checkout nunca van al colector). Verificado: 5 XCTest en
   simulador iPhone + 3 tests JVM en verde. Gotchas pre-existentes
   documentados en los PRs (VioUI no compila para macOS; los demos de
   Kotlin no compilan desde CLI).

Con esto las CINCO superficies hablan el mismo contrato: web (F2), Vev
(F3, PR), servidor (F4, mergeado), iOS/tvOS y Android/TV (F5, PRs).

## Addendum 2 — ClickHouse OSS en Azure (aplicado)

Decisión de Angelo: **OSS self-hosted en vez de ClickHouse Cloud** (escala
de launch, dato en el tenant propio, ~mitad de costo; migrar después =
backup/restore + URL). Aplicado con OpenTofu (11 recursos, state
`vio-analytics.tfstate` en `viotfstate`):

- `vm-clickhouse-vio` (B2s, rg-vio-shared) + disco datos 128GB · CH 24.8 en
  Docker · **TLS automático** (Caddy/Let's Encrypt) en
  `vio-clickhouse.norwayeast.cloudapp.azure.com` · **sin SSH** (ops por
  `az vm run-command`) · backup nocturno de `vio_*` a `saapivio`.
- Una VM, una DB por environment; el colector bootstrapea su schema solo
  (verificado: `vio_development.events` creada al primer arranque).
- **Smoke e2e verde**: colector local → VM Azure → filas con FINAL → API
  `/v1/stats` leyendo de Azure (impressions/purchases/gmv correctos).

Pendiente de Angelo ahora: token+secret de Mixpanel (flag listo en `.env`) y
los `database_url` reales por env para el apply de los Container Apps.
