# 2026-08-26 — F6 en marcha: bundles por audiencia + primera vista real

- Replit demo VIVO en staging (app 4, host replit): funnel hasta
  begin_checkout, 34 eventos también en Mixpanel Staging.
- `/v1/stats/recent`: feed de verificación para integradores (browser +
  `?apiKey=`) — para que Alan chequee tracking sin Mixpanel.
- Inventarios de dashboards (2 agentes): commerce = 19 widgets mock detrás
  de `STATS_API_HOST` vacío + contrato congelado de 5 endpoints; Vio =
  `/analytics` 100% real pero solo engagement, ~10 widgets MUERTOS
  (viewer_count sin writer), proxy #45 sin consumidores, rutas legacy
  `/api/analytics/*` SIN AUTH (pendiente cerrar).
- Debate independencia (Angelo propuso fronts→colector directo): resuelto
  a favor del proxy fino — el directo obligaría al colector a conocer el
  modelo de authz de cada producto (claims/tenancy). Evolución futura si
  hace falta: stats tokens de corta vida emitidos por el backend.
- División quién-ve-qué (Angelo): app/publisher ve su app (operator
  bundle); sponsor/brand ve su catálogo cross-app (sponsor bundle, solo
  token interno). Implementados ambos + vista "Commerce & Impressions"
  real en el drill de app (vio-backend#58; #45 lo mergeó Angelo).
- E2E verificado: dashboard → proxy → colector → VM ClickHouse, con
  ownership validation. Merge de develop en vio-backend trajo dep `jose`.
- Queda: adapter Firebase→sponsor-bundle en base-api (¿quién lo hace?) ·
  modelo de dinero de commerce (decisión producto) · cerrar rutas legacy
  sin auth · widgets muertos (alimentar o retirar) · bug fmtMoney en
  counterparties del webapp.

## Limpieza de pagos en vio-backend staging — Miguel
- Contexto: PR tipiodevelopment/vio-backend#57 (commit 2b6c42d) eliminó rutas de pago y credenciales del backend — todo pago va por Vio Commerce. Ya desplegado y verificado en development; staging quedó atrás (push a main solo dispara development) con env vars/secrets de Klarna aún presentes.
- Hecho:
  - Verificado contra HEAD real de main (no el índice de búsqueda, que estaba stale): rutas y services/klarna.ts efectivamente borrados; deploy.yml confirmado sin set-env-vars.
  - `gh workflow run "Deploy vio-backend" -f environment=staging` → run 32968353832, headSha 2b6c42d, completado.
  - `az containerapp update --remove-env-vars KLARNA_API_BASE KLARNA_API_USERNAME KLARNA_API_PASSWORD` en ca-api-vio-staging.
  - `az containerapp secret remove` para los 3 secrets huérfanos (klarna-api-base/username/password).
  - Verificado: revisión ca-api-vio-staging--0000019, env pago=[], /v2/commerce/klarna/sessions devuelve SPA HTML (no "API key required"), /api/status 200.
- Pendiente: nada.
