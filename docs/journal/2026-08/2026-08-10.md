---
date: 2026-08-10
session: vev-round3-review-scope
participants: [angelo, claude]
status: live
---

# Session — 2026-08-10 — Vev: port ronda 3, review de calidad y alcance de launch

> Continuación del flujo de migración gradual (Alan avanza en el bundle → se porta al
> source). Hoy: ronda 3 portada, review adversarial de todo lo acumulado, y decisión
> de alcance. Estado canónico en
> [`architecture/vio-vev-integration.md §8`](../../architecture/vio-vev-integration.md).

## Qué se hizo

1. **Ronda 3 de Alan portada a `vio-web-sdk`** (`eed82ce`; rebundle vio-vev `119ed30`).
   Contenido: multi-país v1 (país/locale/símbolo por página), matching de variantes
   reescrito (option1/2/3, arrays, títulos, stock multi-formato), validación de
   dirección + fraktmetode obligatoria, Vipps sin formulario, SVG de Stripe corregido.
   `tsc` estricto limpio; bundle vuelve a ser artefacto puro.

2. **Review de calidad de todo lo acumulado** (3 agentes, ~40 hallazgos). Los graves
   son del camino del dinero: confirmación falsa por URL en el retorno de redirect
   (Vipps redirige igual al cancelar), Apple Pay que autoriza sin cobrar, doble
   `CreateCart` que pierde ítems, ítems fantasma con `AddItem` fallido, total del CTA
   sin envío, `resumeKlarnaReturn` sin cablear, colisiones del matching de variantes
   (34/30 → compra 30/32), stock desconocido = "På lager". Resumen en §8.2 del doc de
   arquitectura; inventario accionable en las tareas #4/#5 de la sesión vio-backend.

3. **Fix inmediato**: `ReferenceError` en el picker de productos (import faltante del
   commit "fix prices" de Alan) — `vio-vev` `e01bbf2`.

4. **Decisión de alcance (Angelo): launch = un sponsor, un mercado (NO/NOK).**
   Multi-sponsor, multi-país y multi-supplier POSPUESTOS con inventario completo para
   retomarlos (código se conserva; se activa detrás de flag cuando toque).

## Estado de repos

- `vio-web-sdk` `main` = `eed82ce` (3 rondas de Alan portadas y tipadas).
- `vio-live/vev` `main` = `e01bbf2` (bundle puro + fix picker). Paquete Vev publicado
  (`cq1lXld-TA9`) sigue SIN deployar — espera el hardening.
- Bug EUR/NOK del commerce: **sigue vivo** (variante 408948 → 3310.14, verificado hoy).

## Pendientes (dueños)

- **Nosotros**: batch de hardening del SDK (tarea #5) → luego rebundle + e2e en editor.
- **Angelo**: decidir Apple Pay (apagar vs cablear) · `vev deploy` final · rotar
  `sk_test_` · prueba e2e por método junto a Alan.
- **Alan**: data patch EUR/NOK + purga cache · Klarna habilitado para el sponsor ·
  handoff pendiente (editar `vio-web-sdk/src`, no el bundle).
