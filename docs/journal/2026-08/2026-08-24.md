
## Auditoría staging vio-backend (ex socket-server) — miguel
- Contexto: Angelo reenvió preguntas de un agente de código bloqueado por el pipeline de deploy de `vio-backend` (ex `socket-server`). Se verificó todo en vivo (gh, az, kubectl), sin asumir nada de lo documentado previamente.
- Hecho:
  - Repo renombrado `socket-server` → `tipiodevelopment/vio-backend` (commit d93d81b, 2026-07-27).
  - Confirmado: `deploy.yml` step de migraciones usa `secrets.DATABASE_URL` (Neon, aún vigente) en vez de `DATABASE_URL_STAGING` (Azure PG, correcto, ya existe). Log real: `password authentication failed for user 'neondb_owner'`.
  - Confirmado: ACR `reachuqa2` tiene admin user deshabilitado → UNAUTHORIZED en `docker/login-action`. Ya hay SPs con rol `AcrPush` sobre ese ACR — viable migrar a `az acr login` con la misma identidad usada para AKS (`AZURE_CREDENTIALS`).
  - Confirmado: `drizzle-kit push` v0.31.4 devuelve exit 0 pese a fallo de conexión — el step de migraciones nunca falla el job.
  - Confirmado vía `gh run list`: el pipeline a `develop` (deploy a QA/AKS) solo corrió 4 veces en los últimos 3.5 meses — 2x el 10/06 (falló) y 2x hoy 24/08 (falló). Último deploy exitoso: 08/05. Migraciones 0007/0008 (10/06) nunca se aplicaron vía este pipeline.
  - **Hallazgo crítico no preguntado**: el Deployment `socket-server` NO existe en `kubernetesqa` namespace `default` (ni Service ni Ingress) — el cluster fue reconstruido (~09/07, posterior al último deploy exitoso) y el recurso nunca se recreó. El pipeline apunta a un target inexistente.
  - **Hallazgo crítico**: el staging real que sirve tráfico (`api-staging.vio.live`, HTTP 200) es el Container App `ca-api-vio-staging`, corriendo imagen del commit `a05b99d` (08/06, anterior a Firebase/tenancy). No hay ningún workflow en el repo que despliegue ahí — la revisión activa (creada 20/08) se subió manualmente a un tercer registro (`acrvioapi.azurecr.io`) no referenciado en CI.
  - Confirmado en el Container App real: faltan `COMMERCE_GRAPHQL_URL`, `COMMERCE_GRAPHQL_PUBLIC_URL`, `FIREBASE_PROJECT_ID`, `ADMIN_EMAILS`, `FIREBASE_SERVICE_ACCOUNT_PATH`. Solo `SESSION_SECRET` está presente de las 6 preguntadas.
- Pendiente: decisión de Angelo/Alan sobre target real de staging (AKS vs Container Apps) antes de arreglar el pipeline; confirmar si existen migraciones 0009/0010 en branch sin mergear.
