# 2026-08-21 — Analytics: métricas ajustables, content_title, regla de entornos

Continuación del arco de analytics (ver [2026-08-20-2](./2026-08-20-2.md)).
Sesión Angelo + Claude en vio-backend.

## Métricas por surface — definidas E implementadas

- Pregunta de Angelo: con surfaces siendo iOS/Android/AppleTV/web/SDK/Vev,
  ¿definimos métricas por cada una? → Sí. Doc nuevo
  [`vio-analytics-metrics.md`](../../architecture/vio-analytics-metrics.md)
  (handbook#10): núcleo común + la pregunta de negocio de cada surface
  (web: funnel por `content_url` · apps: retención D1/D7 · TV: fill rate,
  **intent rate** y **conversión cross-device** por `external_user_id`).
- Requisito explícito: **métricas ajustables** ("ahora sé lo básico, con tu
  ayuda puedo acercarme"). Diseño en 3 capas: query params (sin deploy) →
  `src/stats-definitions.ts` (UN archivo con defaults y agrupaciones) →
  redefinición retroactiva (el crudo es nuestro). Contrato de eventos
  rígido, métricas blandas — a propósito.
- Implementados y deployados (dev+staging) los 4 endpoints del backlog:
  `content`, `campaigns/:id/ads`, `cross-device` (`?windowHours`, default
  48h), `retention` (`?cohortDays`, `?surface`). 48 tests; SQL validado
  contra la VM (primera cohorte real: 3 dispositivos del 2026-08-20).

## content_title — reportes a prueba de borrado

Detalle de Angelo: el borrado (de artículos/contenido) es permanente y sin
papelera → la atribución quedaría apuntando a ids ilegibles. Fix en cadena:
`context.content_title` en el contrato (aditivo) + columna ClickHouse con
migración idempotente al boot + `/v1/stats/content` lo devuelve +
**web-sdk 0.5.1** (auto `document.title`, publicado) + **Vev v0.279**
(deployado, timeline verificado). Mismo patrón que `productSnapshot` del
backend. Pendiente aditivo: pasarlo en Swift/Kotlin.

## Regla de emparejamiento api↔events (confirmada)

El agente de Replit reportó 401 del colector: su key vive en el Postgres
LOCAL (usa `api-local-angelo.vio.live` por túnel) pero mandaba eventos a
`events-dev` (que autentica contra cloud dev). No era autorización
pendiente: era cruce de entornos. Solución: hostname nuevo
`events-local-angelo.vio.live` en el túnel `angelo-local-backend` → colector
local :5100 (que guarda en `vio_development` de la VM). **Regla confirmada
por Angelo y fijada en el playbook**: api y events SIEMPRE en pareja del
mismo entorno; el `environment` del SDK la cumple solo.

## Mixpanel MCP (preparado)

Existe MCP oficial de Mixpanel (región EU: `mcp-eu.mixpanel.com/mcp`) con
herramientas de query Y creación de dashboards/cohortes/métricas.
`.mcp.json` del proyecto preparado con auth por Service Account (env var
`MIXPANEL_SA_TOKEN` — Angelo debe crear el service account y exportar el
token). Flujo acordado: explorar/crear boards por MCP (laboratorio) → lo
que demuestre valor se cristaliza en `/v1/stats` + F6 (nuestro).

## Estado de pausas y pendientes

- **F6 proxy (vio-backend#45): EN PAUSA** — Angelo lo quiere revisar antes
  de mergear. No avanzar la vista del dashboard sin su OK.
- Pendientes chicos: token de service account Mixpanel (Angelo) ·
  `contentTitle` en Swift/Kotlin (aditivo) · rebuilds nativos · import TF
  de custom domains.
- Nota operativa: `events-local-angelo.vio.live` depende del colector local
  corriendo en la Mac de Angelo (`pnpm dev` en vio-analytics), igual que
  api-local depende del backend :5001. Si el loop Replit se vuelve
  permanente → convertir a launchd.
