---
date: 2026-08-10
session: woo-vio-wordpress-org-submission
participants: [angelo, claude]
status: live
---

# Session — 2026-08-10 — Woo→Vio: release prep + submitted to WordPress.org

> Continuación del [2026-06-22 (cont.)](../2026-06/2026-06-22-woo-2.md) (E2E validado
> + config-page UX polish). Esta sesión: rename, limpieza del shim legacy, prep de
> release (Plugin Check, assets, ZIP) y **envío del plugin al WordPress.org Plugin
> Directory**. Plugin `vio-woocommerce-sync` @ **`f68cbcb`** (main, pusheado).

## TL;DR

- **✅ ENVIADO a wp.org el 2026-08-10.** Automated scan **PASS**; en cola de revisión
  manual. Slug final **`vio-sync-for-woocommerce`**. Cuenta dueña: usuario **`violive`**,
  email **`angelo@vio.live`** (elegido para que wp.org verifique la marca "Vio" —
  Guideline 17). `Contributors: violive`.
- **Rename** a **"Vio Sync for WooCommerce"** (patrón "… for WooCommerce" = cumple la
  política de marca de wp.org).
- **Shim `reachu-product-id` ELIMINADO** — el backend ya escribe `vio-product-id`
  (commit de Alan `da62d2b`). `reconcile_from_writeback()` fuera; queda el fallback
  `validate-synced`.
- **Plugin Check 0/0** en el slug real (todos los "errores" eran artefactos del nombre
  de carpeta de dev).
- **Columna de producto: badge verde ✓ Synced** (antes mostraba el logo de Vio).
- Nuevos: `uninstall.php`, `LICENSE` GPL-2.0, bulk **Sync / Unsync / Delete from Vio**.
- **Assets `.wordpress-org/`**: 2 banners (772×250, 1544×500) + 2 iconos + 3 screenshots.
- **Túnel Cloudflare arreglado** (ver [lección](../../lessons/cloudflare-tunnel-waf-blocks-wp-admin.md))
  → OAuth connect → **webhooks creados**.

## Estado para retomar (cuando conteste wp.org)

**Esperando el mail** a `angelo@vio.live`, asunto *"[WordPress Plugin Directory] Review in
Progress: Vio Sync for WooCommerce"*. Cola ~445 → días a semanas.

- ⛔️ **NO revocar la API key de staging que se le dio al revisor, ni borrar los 6
  productos de prueba, hasta que aprueben.** El revisor los usa para testear (una tienda
  necesita productos publicados para sincronizar). La key vive en el campo "Additional
  Information" del envío (privado al equipo de revisión) — no se guarda en docs.
- **Si aprueban** → deploy al SVN (ver
  [playbook](../../playbooks/wordpress-org-plugin-release.md)): código a `trunk` + tag
  `1.0.0`, assets de `.wordpress-org/` a `/assets/`. Después **revocar la key** de staging.
- **Si piden cambios** → aplicar sobre `vio-woocommerce-sync` `main`, rebuild ZIP
  (`.distignore`), **no re-subir por el form** — responder el mail del revisor.

## Qué se hizo (release prep)

| Área | Detalle |
|---|---|
| Nombre | "Vio Sync for WooCommerce" (header + readme + text-domain `vio-sync-for-woocommerce`) |
| Shim legacy | `META_LEGACY_PRODUCT_ID` (`reachu-product-id`) removido de `class-plugin.php`, `class-product-mapper.php`, `class-sync.php`; backend escribe `vio-product-id` (Alan `da62d2b`) |
| Reconcile | `reconcile_from_writeback()` fuera; solo `validate-synced` (`reconcile_from_lookup`) |
| ✓ Synced | `Products_Table::render_column` → badge verde con check SVG + "Synced" (title = Vio product id); `.vio-synced` en `admin.css`, columna 92px |
| Bulk actions | `Sync` / `Unsync` (limpia el link local, deja el producto en Vio) / `Delete from Vio` |
| Cleanup | `uninstall.php` (borra options `vio_*` + post-meta `vio-*`), `LICENSE` GPL-2.0, sin `load_plugin_textdomain` |
| Header fix | `Plugin URI` duplicado con `Author URI` → se dejó solo `Author URI: https://vio.live/` (rechazo del form wp.org) |

## Assets wp.org (`.wordpress-org/`)

- `icon-128x128.png`, `icon-256x256.png` (marca Vio negra, transparente).
- `banner-772x250.png`, `banner-1544x500.png` (generados desde el original 2205×713 del
  usuario, ratio ~3.09:1 ≈ el 3.088:1 que pide wp.org).
- `screenshot-1.png` (config/overview conectado), `screenshot-2.png` (lista con ✓ Synced +
  bulk actions), `screenshot-3.png` (sync en progreso). Todos 1647×913.
- Captions en `readme.txt` alineados a 3 screenshots.
- **`.wordpress-org/` NO va en el ZIP del plugin** (lo excluye `.distignore`); va al SVN
  `/assets/`.

## Build / verificación

- ZIP: `rsync` con los excludes de `.distignore` → carpeta `vio-sync-for-woocommerce/` →
  `zip -rX`. 20 archivos, sin ocultos.
- **Plugin Check 0/0** corriendo sobre el build bien nombrado (copiado a `/tmp`, fuera del
  dir de plugins para no chocar con el activo). Los ~83 "ERROR" TextDomainMismatch +
  `hidden_files` + `trademarked_term` que aparecen sobre la carpeta de dev son artefactos
  del nombre `vio-woocommerce-sync` ≠ slug — desaparecen en `vio-sync-for-woocommerce/`.

## Additional Information (lo que se le dijo al revisor)

Propósito (hacer disponibles los productos en apps móviles / live-video shopping vía Vio),
disclosure del servicio externo (Guideline 8: prod `api-ecom.vio.live`, staging
`api-ecom-staging.vio.live`, nada se envía hasta conectar con la key), aclaración de que
**no es trialware** (conector SaaS, no gatea funcionalidad del propio código), y **cómo
testear**: hace falta una tienda con productos + una **API key de staging** (provista) +
pasos connect + "Sync all".

## Commits (pusheados a `origin/main`)

| Commit | Qué |
|---|---|
| `c061b06` | Columna: badge ✓ Synced; banners + 3 screenshots wp.org |
| `f68cbcb` | Fix `Plugin URI`/`Author URI` duplicados (chequeo del form wp.org) |

(sobre `b5fa816` = uninstall + LICENSE + icono + URLs reales de Terms/Privacy, de más
temprano en la sesión.)

## Entorno / gotchas

- **Túnel `woo-dev.vio.live`** — ver
  [lección Cloudflare WAF](../../lessons/cloudflare-tunnel-waf-blocks-wp-admin.md). Sin el
  fix del WAF, el OAuth connect no completa y Vio no crea el webhook.
- El repo `woo-vio` tiene un scaffold vacío aparte, **"Woo Vio"** (v0.1.0, stub
  video/live shopping, montado vía `.` en `.wp-env.json`) — **desactivado** esta sesión.
  NO es el plugin de wp.org; no hacer `wp plugin delete` (bind-mount → borra el repo).
- mu-plugin `zzz-tunnel-proxy` es container-only (no está en el repo, no lo recrea
  `bin/dev.sh`) → un `docker compose down -v` lo pierde.

## Anchors

- Plugin `vio-woocommerce-sync` @ **`f68cbcb`** (main, pusheado a `vio-live/vio-woocommerce-sync`).
- wp.org: slug `vio-sync-for-woocommerce`, cuenta `violive` / `angelo@vio.live`, enviado
  2026-08-10, **Awaiting Review**.
- Env (base-api): prod `api-ecom.vio.live`, staging `api-ecom-staging.vio.live`.
