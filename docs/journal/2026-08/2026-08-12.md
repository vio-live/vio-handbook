---
date: 2026-08-12
session: vev-round5-apple-pay-port
participants: [angelo, claude]
status: live
---

# Session — 2026-08-12 — Ronda 5: Apple Pay conectado al backend (port del trabajo de Alan)

> Alan trabajó hasta la 01:47: "klarna mejora" + 3 commits "apple pay pago"
> (`a496c98`, `1ff7a4a`, `2330878`, `dcedad4`) — todos ediciones a mano del
> bundle, como siempre, pero **esta vez commiteó y pushó** (lección del día 11
> aprendida a medias: siguió deployando ~38 veces, v0.128–v0.166, pero el estado
> final quedó en git). Angelo pidió revisar y migrar.

## Lo que trajo la ronda 5 (y nos responde el contrato de Apple Pay)

1. **Apple Pay cableado al commerce** — las mutaciones que esperábamos:
   - `CreatePaymentApplePay(checkout_id) → {gateway, gateway_merchant_id}`
   - `ConfirmPaymentApplePay(checkout_id, apple_pay_token, email,
     shipping_address: ApplePayAddressInput) → {order_id, status}`
   - Patrón correcto de su parte: el cobro backend corre DENTRO del evento
     authorize del sheet; si falla, el sheet cierra con error.
2. **Klarna**: seleccionar la categoría "pay now" (Betal nå) por defecto en vez
   de la primera/pay_later.

## Lo que NO se portó (atajos inseguros, mismos patrones del review)

- `isSafari()` forzando el botón visible en cualquier Safari (ignora
  `canMakePayment` → botón que abre un sheet roto).
- `launchNativeApplePaySession` con `completeMerchantValidation({})` **vacío**
  — un ApplePaySession sin validación de merchant que siempre falla tras el
  spinner (era el truco para "ver el botón" en vev.site sin dominio).
- `ensureCheckoutAndApplePay` en cada `open()`: fijaba
  `paymentMethod: "Apple Pay"` y aceptaba términos por el usuario en TODOS los
  checkouts, pagara con lo que pagara.
- `gateway_merchant_id` usado a ciegas como publishable key.

## El port (fail-closed, `vio-web-sdk` `7e33e2d`, rebundle `ed9b6c9`, **v0.167**)

- `cart-queries.ts`: las 2 mutaciones nuevas.
- `apple-pay.ts`: hook `onAuthorize` — el cobro corre dentro del authorize del
  sheet; éxito solo si el backend confirma; cancel → `null` (no error);
  handler de `shippingaddresschange` (sin él el sheet se cuelga);
  `preloadStripeJs()` exportado.
- `checkout-manager.ts`: `buildApplePayAuthorize(spId)` compartido por los 3
  botones (checkout, cart express, detalle): asegura checkout →
  `UpdateCheckout(paymentMethod:'Apple Pay'+email)` → `CreatePaymentApplePay`
  (hints `pk_`/`acct_` por prefijo, defensivo) → `ConfirmPaymentApplePay`
  (token+email+dirección mapeada) → status contra `APPLE_PAY_FAILED_STATUSES`.
  Los botones de cart/detalle siguen con prepared-handles (sheet síncrono en el
  tap — la ruta de Alan con awaits antes de `show()` arriesga el bloqueo por
  gesto de usuario de Apple). Stripe.js precargado en el constructor.
- `klarna-payments.ts`: `findPayNowCategory` + default pay_now.
- `vio-checkout.ts`: Apple Pay salta la validación manual de formulario (el
  sheet recoge dirección/envío/email).

## Estado / pendientes

- Visibilidad del botón sin cambios (correcto): lista del backend +
  `canMakePayment` → en `a-vio-dev.vev.site` seguirá oculto (dominio no
  verificable en Vev, ver 2026-08-11). **Probar el cobro real** en
  `vio-demo.vercel.app` (verificado, sirve el archivo de Apple) o en el dominio
  final.
- Incógnitas del contrato a validar en el e2e: semántica exacta de
  `gateway_merchant_id`, valores reales de `status` de Confirm (lista defensiva
  ajustable en `APPLE_PAY_FAILED_STATUSES`).
- Re-publicar el proyecto Vev (la página `/ess` sigue horneada con snapshot
  viejo).
- Tarea #5 (hardening): el wiring del cliente Apple Pay era el único item
  abierto → **hecho**; queda su verificación e2e.

## Panel del Config — estilo Silke + rediseño de campos (misma sesión)

1. **Fix de estilo definitivo** (`5fc27d2`): el input de la API key y el
   selector de sponsor seguían con fondo blanco/dropdown nativo pese al CSS
   inyectado a mano (perdía contra la especificidad del propio CSS de Vev).
   Solución correcta: usar los componentes de **Silke** que usa el mismo Vev
   para sus campos nativos — `SilkeTextField` y `SilkeSelectFieldNative` (los
   mismos que renderizan "API Base"/"Cart position"). Hereda el tema oscuro y
   el dropdown-popover custom automáticamente, sin pelear CSS.
2. **Rediseño de campos** (`592a237`, decisión Angelo — v0.169):
   - **Sin API key por defecto**: `resolveApiKey()` ya no cae a un key demo.
     Sin key pegada, el bloque queda "Disconnected" — resuelve de paso la
     rareza de ayer (Disconnect ya deja un estado realmente vacío).
   - **"API Base" + "GraphQL Base" → un solo dropdown "Environment"**
     (Development/Staging/Production), resuelve ambas URLs juntas:
     - dev: `api-dev.vio.live` / `graph-ql-dev.vio.live`
     - staging: `api-staging.vio.live` / `graph-ql-dev.vio.live` (GraphQL
       compartido con dev — sin staging propio todavía, confirmado)
     - prod: `api.vio.live` / `graph-ql.vio.live`
     Default = staging (preserva el comportamiento de hoy). Hallazgo de paso:
     el propio SDK ya trae un mapa interno de entornos
     (`configuration.ts: DEFAULT_URLS`) pero con la URL de "testing" mal
     apuntada a dev en vez de staging — no importaba porque vio-vev siempre
     sobreescribía `apiBase`/`graphQLBase` a mano (sigue así); no se tocó el
     SDK, solo se corrigió que el campo `environment` que se le manda a
     `Vio.init` ya no queda hardcodeado en `"development"`.
   - **Campo "Stripe PK" eliminado del panel**: ya no se configura desde el
     front. La key de Apple Pay vendrá del backend por sponsor (tarea
     pendiente de Alan — mecanismo exacto aún sin definir por él: candidatos
     son `GetAvailablePaymentMethods().config` o el `gateway_merchant_id` de
     `CreatePaymentApplePay`, que el SDK ya consume defensivamente desde ayer).
     Sin PK estática, Apple Pay queda correctamente no-disponible hasta que
     ese wiring llegue — nunca una key vieja/incorrecta.
