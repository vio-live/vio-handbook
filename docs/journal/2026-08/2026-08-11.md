---
date: 2026-08-11
session: vev-round4-vipps-return
participants: [angelo, claude]
status: live
---

# Session — 2026-08-11 — Ronda 4: verificación del reporte de Alan + fix Vipps fail-closed

> Alan reportó (daily): full test de flows con videos, 2 issues "corregidos" —
> webhook de Vipps y sync de shipping de Klarna a la Order/Shopify. Angelo pidió
> verificar contra código antes de responder.

## Verificación del reporte (barrido org completo, todas las ramas)

1. **Klarna shipping → Order/Shopify: ✅ REAL.** `vio-shopcart` `45b02ac`
   (10-ago 22:29): la línea `shipping_fee` enviada a Klarna lleva
   `reference: shippingId`; al armar la Order, si falta `selected_shipping_option`
   se reconstruye desde `order_lines`. Encaja 1:1 con su descripción. Ojo: envío
   gratis (precio 0) no genera línea `shipping_fee` → ese caso sigue sin fallback.
2. **Vipps webhook: ❌ NO hubo fix de webhook.** Cero commits tocando Vipps en
   ningún repo del org desde el 22-jul. Lo que hizo fue **ronda 4 de edición a
   mano del bundle** (`vev` `5aa8092`, 10-ago 18:15): invirtió la verificación de
   retorno del hardening a fail-open (`paid = true` salvo status explícitamente
   failed/cancelled/rejected) **y lo deployó él mismo** (v0.100 18:15:29, v0.101
   18:24:25) pisando nuestro deploy endurecido.
   - Su diagnóstico era correcto: nuestro check era UNA sola consulta a
     GetCheckout; con webhook lento, una compra real mostraba "kunne ikke
     bekrefte betalingen". Falso negativo real.
   - Su remedio reabría la confirmación falsa del review (cancelación con
     webhook lento → "éxito" + carrito borrado sin cobro).
3. Además esa noche promovió TODO a producción: merges develop→master en los 11
   micros + graphql (00:52–01:30), migración prod en package-database
   (shopify_connection refresh tokens), tomls de vio-shopify-sync.

## Fix nuestro (ronda 4 portada, fail-closed con reintentos)

`vio-web-sdk` `fb60b19` — `checkReturnPaymentStatus` ahora:

- Abre el drawer con notice neutro "Bekrefter betalingen…" y consulta
  `GetCheckout` hasta 5 veces en ~20s (`RETURN_VERIFY_DELAYS_MS`).
- `PAID_STATUSES`/`origin_payment_id` → confirma (igual que antes).
- `FAILED_STATUSES` (failed/cancelled/rejected/expired/…) → error de pago.
- Sigue pending tras los reintentos → notice neutro "Betalingen behandles
  fortsatt…" (carrito intacto, nunca éxito fabricado). Backend inalcanzable →
  mensaje de no-verificado de siempre.
- Nuevo estado UI `paymentNotice` (estilo `.payment-notice` neutro, no rojo).

Rebundle `vio-vev` `0e3cac4` (supersede el hand-edit `5aa8092`) y
**`vev deploy` → v0.102 publicado** (11-ago 11:43), reemplaza la v0.101 de Alan.

## Mensaje para Alan (framing acordado)

- Klarna shipping: perfecto, verificado.
- Vipps: su diagnóstico era correcto (check único demasiado estricto); el fix
  definitivo quedó en el SDK con reintentos + estado pending, fail-closed. Su
  edición del bundle quedó superseded en v0.102 — **recordar handoff: editar
  `vio-web-sdk/src`, nunca el bundle, y no deployar el package sin coordinar.**

## Estado

- `vio-web-sdk` main = `fb60b19` · `vio-live/vev` main = `0e3cac4` · package
  `cq1lXld-TA9` current = **v0.102**.
- Pendientes sin cambio: wiring cliente Apple Pay (espera contrato por Trello),
  e2e por método en staging, rotar `sk_test_`, data patch EUR/NOK (Alan).

## Cómo probar v0.102 (guía para Angelo)

**Setup:** editor de Vev con la cuenta del team **Vio** (no la de tipio — ver
gotcha del 08-10). El package publicado ya es v0.102: NO hace falta `vev start`;
si un proyecto estaba abierto, recargar duro. Verificar en consola que el bundle
no viene de localhost.

**1. Vipps — caso feliz (el fix de hoy):** compra → completar en la app → al
volver debe aparecer el drawer con "Bekrefter betalingen…" y en segundos la
confirmación real. Con webhook lento tolera hasta ~20s reintentando.

**2. Vipps — cancelar (la regresión que evitamos):** iniciar pago → cancelar en
Vipps → volver. NUNCA debe salir "Takk!": o error de pago o, si el status aún no
es cancelled, el aviso neutro "Betalingen behandles fortsatt…" con el
**carrito intacto**. Si sale éxito con carrito borrado, es un bug crítico.

**3. Vipps — webhook muy lento:** si tras ~20s sigue pending → aviso neutro
(no rojo) y carrito intacto. La orden llega igual por email cuando el webhook
procese.

**4. Klarna nativo:** flujo completo con fraktmetode → tras confirmar, revisar
que la **Order en Vio y en Shopify traiga el shipping** (fix de Alan `45b02ac`,
era el gap que él encontró). Caso borde no cubierto: envío gratis (0 kr) sigue
sin fallback — si hay opción de frakt gratis, probarla y anotar.

**5. Stripe (payment link):** pagar → confirmación verificada; cancelar/volver
atrás → sin éxito falso, carrito intacto. (Apple Pay dentro de la página de
Stripe sí es real.)

**6. Apple Pay / Google Pay nativos (botones del SDK): NO usarlos como prueba
de cobro** — completan el sheet sin cobrar (wiring pendiente del contrato de
Alan). Solo verificar que no rompen nada.

**7. EUR/NOK:** la variante seguía "300 EUR" plano al 08-10. Si Alan corrió el
data patch, la variante debe mostrar ≈300 NOK (y ≈27 EUR en EUR). Si sigue
3310+ NOK, el patch no se ha corrido — está en su tarjeta de Trello.

Anotar resultados por método (orden creada / cobro real / email / shipping en
Shopify) — eso cierra el pendiente "e2e por método en staging".

## Gating de botones de pago por backend (v0.107)

Pedido de Angelo: los botones de pago se muestran según lo que responda
`Payment.GetAvailablePaymentMethods`. Ya existía del hardening, pero Apple Pay
lo saltaba en 3 sitios (grid del checkout, nota de Safari, footer del carrito
— solo miraban `canMakePayment`). Ahora TODO botón exige las dos condiciones:
estar en la lista del backend (`methodEnabled`, matching normalizado) Y su
disponibilidad técnica (Apple Pay: canMakePayment; Klarna: lib cargada).
El detalle de producto ya lo hacía bien. `vio-web-sdk` `219f99a`, rebundle
`f720e3b`, **v0.107 publicada**. Semántica: `null` (sin respuesta aún) =
mostrar todo; `[]` = nada; lista = solo esos.

## Decisión: un solo package (Angelo, 2026-08-11)

Inventario de componentes: el package canónico `cq1lXld-TA9` (team Vio) tiene
exactamente los 7 bloques, todos v0.102. El package personal de pruebas de Alan
(`czGNimhyDCi`) duplica los MISMOS 7 nombres congelados al 28-jul
(pre-hardening) con `shareWithAccount:true` → duplicados indistinguibles en el
editor, riesgo de arrastrar la versión insegura.

**Componentes eliminados:** "Vio · Add to Cart" (`e5d36d2`, v0.103) y
"Vio · Open Cart" (`2d30093`, **v0.104 = 5 componentes**: Config, Card,
Carousel, Grid, Open Product Info), deprecados por Angelo. Si alguna página
vieja usaba esos addons, esos elementos pierden el comportamiento (revisar el
demo al probar).

**Config panel (v0.105):** Save/Connect separados en el editor de API key
(Save persiste el prop; Connect fuerza reconexión con la key del campo vía
`vio:connect-request`, sin guardar), Disconnect a fila propia, hint de dirty
state, y fuera los console.log de debug del sponsor selector (`f60d448`).
Verificado de paso: `stripePublishableKey` solo alimenta el Apple Pay nativo
(apple-pay.ts vía Stripe.js) — el flujo Stripe real va por payment-link del
backend sin tocar la PK; y analytics GA4/dataLayer + inspector están
funcionales (falta GTM/GA4 del publisher para que salgan de la página).

**Decisión: `cq1lXld-TA9` es el ÚNICO package.** `vev-alan.json` eliminado del
repo (`1c4ff6f`) + política en el README. **Limitación de Vev:** el package de
Alan vive en su cuenta personal (`x7TKEcyu…`) — no se puede transferir ni
des-compartir desde fuera; tiene que hacerlo Alan (`shareWithAccount:false` +
redeploy, o dejarlo morir sin uso). Pendiente decírselo (va con el mensaje del
handoff).
